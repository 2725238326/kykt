# CYCLE-20260505-006 cycle 015 launch and Critic L3 pilot scope locked

cycle_id: CYCLE-20260505-006

cycle_number: 015

date_opened: 2026-05-05

date_closed: open

status: in_progress (S1..S2 done; S3..S5 active; S6..Sk gated on per-step micro gates)

owners: agent (markdown-only sync), user (per-step execution gates)

trigger:

```text
User selected "授权 Critic L3 窄域 pilot" as the cycle 015 entry, in
response to the agent's recommendation surfaced after cycle 014 closeout
in cycles/CYCLE-20260505-005.md "Cycle 015 launch packet (deferred to
user)". This satisfied the Open user decision phrased in
planning/L3_PILOT_SELECTION.md line 167-171.
```

parent_decision: decisions/DEC-20260505-005-cycle-015-launch-critic-l3-pilot.md

prior_cycle: cycles/CYCLE-20260505-005.md (cycle 014 closed clean; idle hand-off)

## Cycle 015 scope reminder (from DEC-20260505-005)

Allowed by DEC-20260505-005:

```text
clone Test3R + CTRL + DUSt3R + MASt3R;
download checkpoints required for one smoke loop on one hard-case;
install minimum env on a single box;
run one smoke loop end-to-end;
emit one JSONL log line per (input, S, reroute, revised output, delta);
write a thin wrapper dream_critic_loop.py and a hand-derived
capability_match YAML.
```

Not allowed by DEC-20260505-005 (excerpt):

```text
no full benchmark sweep; no training; no KYKT navigation change;
no frontend; no storyboard promotion; no G2 closure claim;
no retroactive case-card edits; no system-level changes;
no silent upstream patches.
```

Per-step micro gates (each requires user go in active conversation): G_clone, G_install, G_download, G_run, G_log_use.

## Cycle 015 subtask board

| ID | Subtask | Status | Canonical artifact |
| --- | --- | --- | --- |
| S1 | Write DEC-20260505-005 (cycle 015 launch + scope authorization) | done | `decisions/DEC-20260505-005-cycle-015-launch-critic-l3-pilot.md` |
| S2 | Write this cycle log (cycle 015 cycle log + initial subtask board) | done | `cycles/CYCLE-20260505-006.md` |
| S3 | Update TASK_SNAPSHOT.md FIRST per F-001 rule 6 (Status -> in_progress; cycle 015 subtask board; resume pointer) | done | `TASK_SNAPSHOT.md` |
| S4 | Sync chain: WORKFLOW_STATUS.md + RESEARCH_STATE.md + INDEX.md + registry/decision_registry.md + AGENT_MASTER_PROMPT.md + README.md | done | sync chain |
| S5 | Surface G_clone micro gate to user (proposed pilot path; proposed hard case) | done | this cycle log + user message |
| S6 | G_clone returned `go` 2026-05-05 (default path + 4 repos). Executed: pilot path created; `git clone --depth 1` Test3R + CTRL + DUSt3R + MASt3R; 28 MB total; license sniff = CC BY-NC-SA 4.0 (Naver: Test3R/DUSt3R/MASt3R) + Apache 2.0 (critic-rl); research use OK; commercial-path constraint recorded. | done | `experiments/runs/cycle-015-critic-l3-pilot/` |
| S7 | Surface G_install micro gate to user (server-side framing after corrigendum); user reply `Go (server-side install Test3R only)`. | done | this cycle log + user message |
| S8 | G_install (revised) `go`. Server github.com blocked; user chose scp; local Test3R uploaded (18 MB, HEAD a2eb94b matches); conda env `test3r` built; conda pytorch hit MKL symbol error -> within-scope fixup: switched to pip torch 2.5.1+cu121 (matches dust3r env on same server); Test3R requirements installed; sanity passed (4x TITAN RTX 24GB; CUDA 12.1; all deps import). | done | server `/hdd3/kykt26/code/Test3R/` + `test3r` conda env |
| S9 | G_download surfaced; HF blocked but Test3R weight = same DUSt3R 2.2 GB ckpt already on server. User chose `Patch launch.py 指向已有 ckpt`. Executed: backup + sed-patch line 103 to local path; verified `from_pretrained` loads 571.2M params, all keys matched, CUDA OK; no HF call. Degenerate G_download (no actual download). | done | server `/hdd3/kykt26/code/Test3R/eval/mv_recon/launch.py` + `.cycle015.bak` |
| S10 | Surface G_run micro gate to user (smoke loop scope: hard-case input + Critic conflict score + Composer-backed reroute + delta log; agent will draft markdown design of dream_critic_loop.py before code) | in_progress | this cycle log + user message |
| S11+ | Reserved for execution sub-passes; G_log_use -> S11 | not started | TBD |

## Activation pass log (S1)

What was done in S1:

```text
DEC-20260505-005 written from scratch as a NEW file. It cites:
- planning/L3_PILOT_SELECTION.md "Recommended first-pilot scope" verbatim,
- experiments/EXP-20260505-001 prerequisite inventory,
- v2.1 contract and CRITIC-01..03 case cards,
- the user's authorization phrasing in this session.
DEC explicitly lists 10 "not allowed" items, 5 micro gates, 7 inherited
+ 3 cycle-015-specific stop conditions, and 7 acceptance criteria.
DEC produces zero L3 evidence; carries `user-decided` label for the
authorization itself; all operational numbers stay `inferred`.
```

Why a separate DEC was required even though the user verbally authorized in this session:

```text
TASK_SNAPSHOT.md "If interrupted, resume from" item 4 explicitly says
"Do NOT execute it without a separate user-approved DEC." Verbal
authorization is necessary but not sufficient; the DEC is the durable
artifact a future agent / a future session can read to know what was
authorized and what was not. DEC-20260505-005 is that artifact.
```

## Activation pass log (S2)

What is being done in S2:

```text
This cycle log is being written. It mirrors the discipline established
by CYCLE-20260505-005 (cycle 014 closeout), so a future agent reading
just the cycle log can see (a) what was authorized, (b) what micro
gates remain, (c) what stop conditions apply, (d) which subtasks are
done vs. pending.
```

## What S3 will do (TASK_SNAPSHOT.md update)

```text
- Flip Status from `idle` (cycle 014 closed) to `in_progress` (cycle 015
  launch + sync).
- Update top-of-file "Last updated" stamp to 2026-05-05 with cycle 015
  launch tag.
- Replace the "Current task" block to reflect cycle 015.
- Replace the "Subtask board" with the cycle 015 board above.
- Update "If interrupted, resume from" to point at this cycle log.
- Add cycle 015 launch decision summary under "Open user decisions"
  (preserving cycle 011 / 012 / 013 / 014 history; nothing deleted).
- Move the cycle 014 entry to "Last completed task pass".
```

S3 is FIRST in the F-001 rule 6 sync chain so an interrupt mid-pass still leaves a valid resume pointer.

## What S4 will do (rest of sync chain)

```text
- WORKFLOW_STATUS.md: current cycle pointer to cycle 015; reference
  DEC-20260505-005.
- RESEARCH_STATE.md: active cycle = 015; active gates = G_clone +
  inherited project gates; goals G2 / G6 / G7 unchanged.
- INDEX.md: add row for DEC-20260505-005 and CYCLE-20260505-006.
- registry/decision_registry.md: add DEC-20260505-005 row.
- AGENT_MASTER_PROMPT.md: skip unless cycle-015 changes the master
  prompt's hard-rule list; on first inspection it does not. Decide at
  S4 read time.
- README.md: skip unless README has a current-cycle pointer that needs
  updating. Decide at S4 read time.
```

## What S5 will do (G_clone micro gate)

The agent will surface a single message to the user containing:

```text
Proposed pilot path        : E:\kykt\Dream\experiments\runs\cycle-015-
                              critic-l3-pilot\
Proposed repos to clone    : Test3R, CTRL, DUSt3R, MASt3R
                              (URLs from EXP-20260505-001 section a)
Proposed hard-case input   : (one specific image pair / triplet pulled
                              from a public hard-case source aligned
                              with CRITIC-01 failure taxonomy; agent
                              will name the candidate and let user
                              redirect)
Proposed env tooling       : conda env (or venv) on a single GPU box;
                              user must confirm GPU presence / spec
Question to user           : "Go to clone these 4 repos under <path>
                              now? Or redirect path / repo set?"
```

Until the user replies "go" or equivalent at S5, NO clone, NO download, NO install, NO run.

## Stop conditions for this cycle

Inherited from DEC-20260505-005 (which inherits from EXP-20260505-001):

```text
(a) any URL 404
(b) license blocks
(c) wall-clock blow-out (10x inferred)
(d) privileged system change required
(e) micro gate "no" or "redirect"
(f) output path drift
(g) smoke produces no signed delta
```

## Open items at S2 close

```text
1. S3 about to start (TASK_SNAPSHOT.md update FIRST per F-001 rule 6).
2. After S5, if user gives G_clone go, cycle 015 enters its first
   execution sub-pass (S6 = clone). Otherwise this cycle stays gated.
3. CASE-20260504-CRITIC-01 evidence label remains `inferred`. Will
   only upgrade on full S6..S_run success and a separate G_log_use
   gate.
4. STORY-20260505-001-critic.md remains `draft`. No promotion.
5. v2.1 contract unchanged. No v2.2 candidate surfaced this cycle.
6. G2 / G6 / G7 status unchanged at this point of cycle 015.
```

## Cycle 015 audit note (S3..S5 will be appended below at end of pass)

To be appended when S3..S5 land. The pattern mirrors cycle 014's
"Cycle 014 audit (S5)" and "Cycle 015 launch packet (deferred to
user)" subsections in CYCLE-20260505-005.md.

## S6 execution log (G_clone authorized, default path + 4 repos)

User reply at S5: `Go (default path + 4 repos)`. Authorization recorded
in TASK_SNAPSHOT.md and DEC-20260505-005 (G_clone gate).

Execution:

```text
mkdir -p E:\kykt\Dream\experiments\runs\cycle-015-critic-l3-pilot
git clone --depth 1 (4 repos in parallel)
```

`--depth 1` chosen to keep code-only disk small for the smoke pilot.
HEAD-only history is sufficient for L3 evidence; full history would be
unshallowed at G_download time only if any checkpoint download script
required full history.

Per-repo measurements (replaces inferred numbers from
EXP-20260505-001 §b for the code-only portion; checkpoints not
yet downloaded):

| Repo | Code size | Files | HEAD commit | HEAD subject | License |
|---|---|---|---|---|---|
| Test3R | 18 MB | 164 | `a2eb94bc716df27521f053d417fcf6afa9870ff5` | Update README.md | CC BY-NC-SA 4.0 (Naver, inherited from DUSt3R) |
| critic-rl (CTRL) | 465 KB | 67 | `13dc7109ee60ebc7ac13ebf49706db1cfe550e22` | Update README.md | Apache 2.0 |
| dust3r | 2.0 MB | 125 | `4c24a6ebf04809f2cfe59915e51779c8984aaa40` | add link to ScanNet++ v2 pairs | CC BY-NC-SA 4.0 (Naver) |
| mast3r | 7.7 MB | 92 | `f5209afc300cec36239a7ac992263f36847bbba0` | Fix typo in LICENSE | CC BY-NC-SA 4.0 (Naver) |
| **total** | **28 MB** | **448** | — | — | — |

Evidence label upgrade: code-only disk per-repo size is now `measured`
on this box (RTX-class Windows 11 host, 2026-05-05). EXP-20260505-001
§b inferred 0.5-2 GB code per repo and ~20-40 GB for 4 repos; the
measured 28 MB total is much lower because `--depth 1` was used and
the dominant disk cost is the not-yet-downloaded checkpoints. The
GPU / wall-clock numbers in EXP-001 §b remain `inferred`; only the
code-clone disk number upgrades.

License finding (significant; surfaced before G_run):

```text
Test3R + DUSt3R + MASt3R: CC BY-NC-SA 4.0 (Creative Commons
  Attribution-NonCommercial-ShareAlike 4.0).
critic-rl: Apache 2.0.

For the L3 research pilot (cycle 015 scope per DEC-20260505-005),
research use is permissive under CC BY-NC-SA. Stop condition (b)
"License on any checkpoint or repo prohibits the intended research
use" is NOT triggered.

For any later commercial KYKT path that ships Critic-derived code
or weights touching DUSt3R / MASt3R / Test3R, the NonCommercial term
becomes load-bearing. This is OUT OF SCOPE for cycle 015 and is
recorded here so a future commercial-path DEC has the constraint
on file from day 1.

ShareAlike clause: any redistribution of derived code under CC
BY-NC-SA must inherit the license. The thin orchestration wrapper
dream_critic_loop.py (per DEC-005) is a separate file that imports
these libraries; whether ShareAlike attaches depends on
distribution and combination details, which are NOT load-bearing
for the smoke pilot.
```

Stop conditions checked at end of S6:

```text
(a) URL 404                       : not triggered (all 4 repos live)
(b) license blocks research use   : not triggered (CC BY-NC-SA 4.0
                                    permits research; Apache 2.0
                                    permissive). Commercial-path
                                    constraint recorded above.
(c) wall-clock blow-out           : not triggered (4 parallel clones
                                    completed in seconds; well under
                                    inferred 1-2h env-setup envelope
                                    which covers install + clone)
(d) privileged system change      : not triggered (no driver / CUDA /
                                    kernel changes; only mkdir + git)
(e) micro gate "no" or "redirect" : not triggered (G_clone returned go)
(f) output path drift             : not triggered (clones landed at
                                    agreed pilot path)
(g) no signed delta               : N/A (smoke not yet run)
```

Per DEC-20260505-005 explicit "Not allowed": no install, no
checkpoint download, no run, no edits to upstream sources, no
storyboard promotion, no G2 closure claim. None of these took place
during S6.

S6 outcome: clone-only execution succeeded; license constraint surfaced
for future commercial-path awareness; ready to surface G_install.

## What S7 will do (G_install micro gate)

The next surfaced gate is G_install. It will ask:

```text
- env tooling: conda env named cycle-015-critic-l3-pilot OR
               python -m venv at the pilot path? (default: conda
               since the 4 repos' READMEs lean on conda for CUDA
               build pinning; user can redirect)
- Python:      proposed 3.11 (DUSt3R / MASt3R README defaults; user
               can redirect)
- CUDA:        check the local nvidia-smi + nvcc to propose a CUDA
               version that matches the local GPU driver; do NOT
               install / downgrade drivers (stop condition (d))
- per-repo:    install per-repo requirements.txt for DUSt3R + MASt3R
               + Test3R; CTRL is a pattern reference and may not
               need installing
- editable:    pip install -e on dust3r and mast3r so the wrapper
               can import them as python packages
- wrapper:     no wrapper code yet at G_install; that comes after
               G_run gate authorizes execution
```

The G_install gate message will surface concrete proposals (Python
version, CUDA detected, env name, per-repo install plan) and let the
user approve / redirect each item.

## S7 pre-flight: local environment probe (informational; no install)

Before surfacing G_install, the agent probed the local environment so
the gate question carries concrete proposals. NO install / NO env
creation took place during the probe.

Probe result (measured 2026-05-05 on this Windows 11 box):

```text
GPU         : NVIDIA GeForce RTX 4060 Laptop GPU
              VRAM             : 8 GB total (8188 MB)
              Currently used   : ~2 GB (Windows desktop)
              Available for ML : ~6 GB at probe time
              Driver           : 596.36 (CUDA capability 13.2)
nvcc        : not on PATH (system-level CUDA toolkit not installed;
              fine — PyTorch ships its own CUDA via conda)
conda       : 24.11.3 (envs default at D:\Anacondaenv\envs\)
existing envs: base, 3.2, BUAA_TeachingEvaluation_2024Winter-main,
              Lesson, SBzhs, automate (all unrelated to cycle 015)
system Python: 3.13.5 (Python.org Windows install)
              Note: repos want Python 3.11 specifically; system 3.13
              cannot be used for the env. A fresh conda env is required.
```

Repo install instructions (read-only inspection of each repo's README):

```text
DUSt3R (dust3r/README.md lines 70-80):
  conda create -n NAME python=3.11 cmake=3.14.0
  conda activate NAME
  conda install pytorch torchvision pytorch-cuda=12.1 -c pytorch -c nvidia
  pip install -r requirements.txt
  Optional: requirements_optional.txt, RoPE CUDA kernels.

MASt3R (mast3r/README.md lines 90-105):
  Identical recipe. Then:
  pip install -r requirements.txt           (mast3r-specific deps)
  pip install -r dust3r/requirements.txt    (vendored dust3r subdir)
  Optional: ASMK build, dust3r/requirements_optional.txt, RoPE.

Test3R (Test3R/README.md lines 24-29):
  Identical recipe. Then:
  pip install -r requirements.txt
  Optional: RoPE CUDA kernels.

critic-rl / CTRL:
  Pattern reference only per EXP-20260505-001 §a (LM domain);
  not in the smoke-loop critical path. Recommend SKIP install
  unless user redirects.
```

Critical finding (must surface in G_install gate before user `go`):

```text
EXP-20260505-001 §b inferred GPU class = 24 GB (RTX 3090 / 4090 /
A5000). Measured local GPU = 8 GB (RTX 4060 Laptop). This is a
real delta, NOT a stop-condition trigger by itself, but it
constrains the smoke loop:

- DUSt3R / MASt3R at default 512 resolution may exceed 8 GB
  on a 2-image forward (model weights ~2.8 GB fp16 + activations).
- Practical smoke resolution will likely need to drop to 384 or
  224 to fit available VRAM after Windows desktop reservation.
- This is a planning fact for G_run; the install itself is not
  blocked by it.

EXP-20260505-001 §b inferred wall-clock 1-2 hours for env setup
on a clean box. Local box is not clean (conda already installed,
many envs already exist), so the wall-clock should be dominated by
PyTorch + CUDA download (~3-5 GB) for one fresh env. Estimate:
30-90 minutes wall-clock for the install itself, dominated by
network I/O. This number remains `inferred` until the install runs.

EXP-20260505-001 §b inferred disk: 0.5-2 GB code per repo +
1-5 GB checkpoints. Code-only reality measured at S6: 28 MB total
(--depth 1). Env disk delta on first install is the next dominant
cost: PyTorch + CUDA + deps typically land ~10-20 GB in the env
directory. Checkpoints come at G_download.
```

Stop conditions checked at end of S7 pre-flight:

```text
(a) URL 404                       : not triggered (no URLs touched
                                    during probe)
(b) license blocks research use   : not triggered (probe is
                                    inspection only)
(c) wall-clock blow-out           : N/A (no install ran)
(d) privileged system change      : NOT triggered. The probe did
                                    NOT touch driver / CUDA /
                                    kernel. PyTorch CUDA 12.1 is
                                    forward-compatible with the
                                    measured driver 596.36 (CUDA
                                    capability 13.2); no driver
                                    change needed.
(e) micro gate "no" or "redirect" : N/A (G_install not yet asked)
(f) output path drift             : not triggered
(g) no signed delta               : N/A (smoke not yet run)
```

S7 pre-flight outcome: probe data sufficient to compose a concrete
G_install gate question. No install ran; no env was created; no
package was downloaded. Ready to surface G_install.

## S7 corrigendum: server-side topology correction (F-002)

The G_install proposal surfaced from the local probe above was wrong.
The user immediately corrected:

```text
"不是啊，我们的模型都是在服务器上运行的呀，包括虚拟环境都在那
边创建，你知道放在哪吗"
```

Translation: KYKT 3R models, including all conda virtual environments,
run on a remote server, NOT on the local Windows box. The local Windows
machine the agent probed is an orchestration / markdown station only.

Recorded as F-002 in TASK_SNAPSHOT.md "Recent failure modes". Persisted
as a feedback memory at
`C:\Users\27252\.claude\projects\e--kykt\memory\feedback_kykt_server_topology.md`
so future sessions and agents do not re-make the same mistake.

Canonical topology references (the agent should have consulted these
BEFORE composing the local-install G_install proposal):

```text
E:\kykt\WORK.md
  - section "5. Why SSH / SCP Was Chosen" + section "6. Standardize
    remote runner format"
  - existing remote runners listed: dust3r_runner.py /
    mast3r_runner.py / monst3r_runner.py / spann3r_runner.py /
    fast3r_runner.py
  - canonical job dispatch:
      python /hdd3/kykt26/code/vision_runner/run_job.py
            --job /hdd3/kykt26/jobs/<job_id>/job.json
  - communication: system ssh + scp, no inbound service
  - local helper: E:\kykt\Coding\4.06\vision_ui\ssh_runner.py

E:\kykt\.omx\plans\kykt-app-backend-model-integration-plan.md
  - server work dir: /hdd3/kykt26/
  - runner registry pattern: model_registry.py + ssh_runner.py +
    per-model runner scripts
  - planned RunnerDispatchSpec contract (Phase 2 of that plan)
```

Implications for cycle 015:

```text
1. Of the 4 cloned repos, DUSt3R + MASt3R already have production
   server runners (dust3r_runner.py + mast3r_runner.py) with envs
   + checkpoints almost certainly already in place server-side.
   The cycle-015 pilot should REUSE those, not re-install them.

2. Test3R and CTRL are NEW (not in the existing runner inventory).
   These are the only two that need server-side install work.
   Test3R is the in-family geometric self-check baseline used by
   the Critic A4 verification step; CTRL is a pattern reference
   only and likely does not need install.

3. The Critic orchestration wrapper (dream_critic_loop.py per
   DEC-005) should be a server-side script that the local app
   dispatches over ssh, NOT a local Python script. This is the
   shape that fits the existing runner pattern.

4. Existing server-side checkpoints for DUSt3R + MASt3R remove most
   of the G_download burden. The remaining download is the
   Test3R-style consistency objective checkpoint (paper-provided
   per EXP-20260505-001 §a).

5. The local 8 GB RTX 4060 Laptop GPU is irrelevant; the actual
   GPU spec is whatever the server provides. Re-asking the user
   for that spec is one of the items the revised G_install gate
   needs to surface.

6. The 4 shallow clones already on the local Windows box stay
   useful: code reading, README inspection, paper-tracking, and
   composing the orchestration wrapper offline. They are NOT the
   execution copies of the code.
```

Stop conditions checked at end of S7 corrigendum:

```text
(a) URL 404                       : not triggered
(b) license blocks research use   : not triggered
(c) wall-clock blow-out           : N/A
(d) privileged system change      : not triggered
(e) micro gate "no" or "redirect" : G_install proposal redirected
                                    by user; revised proposal
                                    drafted under server-side
                                    framing; gate to be re-asked
(f) output path drift             : not triggered (no execution
                                    happened)
(g) no signed delta               : N/A
```

Cycle 015 contract not violated: DEC-20260505-005's "not allowed"
list (no full sweep, no training, no system-level changes, ...)
was not breached because the only execution that took place was
the G_clone shallow clones; the G_install proposal was a markdown
proposal, never executed.

## What S7 (revised) will ask under server-side framing

The revised G_install gate question must surface:

```text
- server SSH host / address (agent does not know it; ask user)
- server work dir for cycle-015 pilot:
    proposed: /hdd3/kykt26/dream-experiments/cycle-015-critic-l3-pilot/
    (sibling of /hdd3/kykt26/code/vision_runner/ + /hdd3/kykt26/jobs/)
    user can redirect
- env reuse:
    DUSt3R server-side env: does it already exist? (likely yes via
      dust3r_runner.py); reuse or fresh?
    MASt3R server-side env: same question
    Test3R: NEW; needs fresh env (proposed test3r conda env on server)
    CTRL: SKIP install (pattern reference only)
- env strategy on server:
    conda? venv? does the existing server have a convention?
    user input required
- per-repo deploy:
    push the 4 shallow clones to the server pilot path via scp,
    OR re-clone on the server (server probably has git);
    agent recommends server-side clone since shallow clones make
    --depth 1 forwarding awkward
- next gates queued:
    G_download: only for the Test3R-style checkpoint (DUSt3R +
      MASt3R checkpoints expected to exist server-side)
    G_run: dispatch the Critic smoke loop via ssh_runner.py
    G_log_use: scp results back to local
```

Until the user answers the revised G_install gate, no install / no
download / no run takes place.

## S7 server coordinates resolved (read-only inspection of ssh_runner.py)

User redirected the agent to read `ssh_runner.py / 环境变量` rather than
hand the agent SSH details verbatim. The agent read
`E:\kykt\Coding\4.06\vision_ui\ssh_runner.py` lines 22-44 and extracted
the canonical `ServerConfig` dataclass. NO ssh connection attempted; NO
network call; NO server-side file touched. Pure read-only inspection.

Resolved server coordinates (canonical, hardcoded in ssh_runner.py):

```text
SSH alias  : KYKT-UI
host       : 172.17.140.97
user       : kykt26
port       : 22
SSH options: BatchMode=yes; ConnectTimeout=12;
             ServerAliveInterval=20; ServerAliveCountMax=2

Server work dir : /hdd3/kykt26/
  jobs dir      : /hdd3/kykt26/jobs/
  runners dir   : /hdd3/kykt26/runners/

Per-runner anchors (envs + repos + checkpoints already exist server-side):

  DUSt3R  : repo  = /hdd3/kykt26/code/dust3r-main/
            ckpt  = /hdd3/kykt26/models/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth
            env   = dust3r

  MASt3R  : repo  = /hdd3/kykt26/code/mast3r/
            ckpt  = /hdd3/kykt26/code/mast3r/checkpoints/MASt3R_ViTLarge_BaseDecoder_512_catmlpdpt_metric.pth
            env   = mast3r

  MonST3R : repo  = /hdd3/kykt26/code/monst3r/
            env   = monst3r

  Spann3R : repo  = /hdd3/kykt26/code/spann3r/
            ckpt  = /hdd3/kykt26/code/spann3r/checkpoints/spann3r.pth
            env   = spann3r

  Fast3R  : repo  = /hdd3/kykt26/code/fast3r/
            ckpt  = /hdd3/kykt26/models/fast3r/Fast3R_ViT_Large_512/
            env   = fast3r
```

Implications now firmly established (not inferred):

```text
1. DUSt3R env + checkpoint exist server-side. The 512-resolution
   default checkpoint (DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth)
   matches the cycle-015 smoke target.
2. MASt3R env + checkpoint exist server-side. The metric variant
   (catmlpdpt_metric) matches the cycle-015 reroute target.
3. Test3R is NOT in the runner inventory. The cycle-015 pilot
   needs to add a Test3R server-side env, repo, and checkpoint
   download.
4. CTRL / critic-rl is also NOT in the runner inventory. Per
   cycle 015 scope, CTRL is pattern reference only; no server-side
   install required for the smoke.
5. The Critic orchestration wrapper (dream_critic_loop.py) should
   be a NEW server-side script under /hdd3/kykt26/runners/ or a
   new /hdd3/kykt26/code/dream_critic/ subdir, dispatched via the
   existing ssh_runner.py pattern. NOT a local Python script.
6. Local 4 shallow clones stay useful for code reading + wrapper
   drafting, but they are NOT pushed to the server (server can git
   clone independently if needed for Test3R; existing repos are
   already in place).
```

Stop conditions checked at end of S7 server-coords-resolution:

```text
(a) URL 404                       : not triggered (no URL touched)
(b) license blocks research use   : not triggered
(c) wall-clock blow-out           : N/A (no install ran)
(d) privileged system change      : not triggered (read-only file
                                    inspection of one local Python
                                    file)
(e) micro gate "no" or "redirect" : N/A (server-coord lookup is
                                    inside the redirected G_install
                                    flow)
(f) output path drift             : not triggered
(g) no signed delta               : N/A
```

Ready to surface the revised G_install gate with concrete proposals.

## S8 execution log: SSH probe + clone failure (network blocker)

User reply at S7: `Go (server-side install Test3R only)`. S8 entered.

### Probe results (all read-only; no mutation)

```text
ssh KYKT-UI -> exit 0 (host buaaliubo-SYS-7049GP-TRT;
                       Linux Ubuntu 20.04;
                       Linux 5.15.0-139)
GPUs        : 2 x NVIDIA TITAN RTX, 24 GB VRAM each, both idle.
              EXP-20260505-001 §b inferred 24 GB-class; this matches.
              Local 8 GB RTX 4060 GPU is irrelevant.
nvidia-smi  : driver 580.82.09; CUDA capability 13.0
              (PyTorch CUDA 12.1 forward-compatible; no driver change)
conda       : 25.11.0 at /usr/local/bin/conda
existing envs: align3r, cut3r, dust3r, fast3r, kykt, mast3r,
              monst3r, sfm, spann3r (9 total). The runner inventory
              in ssh_runner.py:22-44 listed 5; align3r / cut3r /
              kykt / sfm are additional and unrelated to cycle 015.
              Test3R env name candidate: free.
/hdd3 free  : 3.6 TB free of 15 TB (74% used). Abundant for one
              ~10-20 GB env + later checkpoints.
/hdd3/kykt26/code/ contents:
              dust3r-main, mast3r, monst3r, spann3r, fast3r,
              align3r, cut3r, Old/ (no Test3R yet -> clean).
/hdd3/kykt26/dream-experiments/: did not exist (mkdir-able).
```

Probe outcome: stop conditions (a)-(g) all clear at probe time.

### Clone attempt + failure

```text
attempt 1: server-side `git clone https://github.com/nopQAQ/Test3R.git`
  output : "GnuTLS recv error (-54): Error in the pull function" /
           "remote end hung up unexpectedly" / "early EOF"
  result : FAIL; partial clone wiped (rm -rf) before retry.

attempt 2: `git -c http.version=HTTP/1.1 -c http.postBuffer=524288000
            clone --depth 1 https://github.com/nopQAQ/Test3R.git`
  output : "GnuTLS recv error (-110): The TLS connection was
           non-properly terminated."
  result : FAIL.

diagnosis (read-only probes after the clone failures):
  DNS                : OK; getent resolves github.com to 20.205.243.166
  curl -I https://github.com -m 10 : timeout after 10001 ms
  no proxy env vars  : http_proxy / https_proxy not set
  git --version      : 2.25.1 (older, but flag retry made no difference)
  existing dust3r-main: not a git repo at all (no .git dir);
                       previously deployed by some other mechanism
                       (likely scp from another machine or local
                       checkout, not direct github clone).

conclusion: the server cannot reach github.com over HTTPS (TLS
            connection cuts mid-stream / curl times out). This is
            an environmental block, not a flags-fix problem. The
            existing /hdd3/kykt26/code/dust3r-main/ etc. were not
            placed there via direct github clone either; some other
            transfer path is in use that this agent has not been
            told about.
```

### Stop condition handling

```text
Per DEC-20260505-005 + cycle-015 stop conditions:
(a) URL 404                       : not triggered (URL resolves
                                    via DNS; the github.com host
                                    just refuses HTTPS data flow
                                    for this server)
(b) license blocks                : not triggered
(c) wall-clock blow-out           : not triggered (clone failed
                                    in seconds, not 10x estimate)
(d) privileged system change      : would be triggered if the
                                    "fix" were to install a
                                    network-level workaround
                                    (proxy daemon, route change,
                                    /etc/resolv.conf edit). The
                                    agent did NOT do that.
(e) micro gate "no" or "redirect" : not yet (G_install gate is
                                    still authorized at the SCOPE
                                    level; only the network
                                    primitive failed)
(f) output path drift             : not triggered (no files
                                    written outside the agreed
                                    pilot path; the failed clone
                                    left no partial dir)
(g) no signed delta               : N/A
```

The agent did not invent a workaround. Per DEC-005 + the
cycle 015 spirit, the next step is to surface this concrete
network blocker to the user with two non-improvised options:

```text
Option A: scp the local shallow clone up.
  The agent already cloned Test3R locally at S6 to
  E:\kykt\Dream\experiments\runs\cycle-015-critic-l3-pilot\Test3R\
  (18 MB, --depth 1, HEAD a2eb94b). One `scp -r` upload bypasses
  the github outage on the server side. If the install later
  needs full git history, `git fetch --unshallow` would still
  hit the same outage, but for the smoke loop --depth 1 is
  enough.
  Risk: if Test3R install scripts call back to github (e.g. for
  submodules, pip deps from git, or weights URLs), those will
  fail too at install / G_download time.

Option B: use the same transfer mechanism that placed
  dust3r-main / mast3r / etc. on the server.
  The agent does not know this mechanism. Likely candidates:
  scp from a different machine that does have github access; an
  internal mirror; a manually downloaded zip pushed via web.
  The user knows; the agent does not.

Option C: hold S8.
  Cycle 015 stays at S7 done / S8 surfaced. F-002 + memory +
  corrigendum already in place. User answers later when they
  are ready to look at the network situation.
```

S8 status: gated on this network finding. No additional clone
attempt, no install, no env create, no checkpoint download.

## S8 resolution: scp + install (2026-05-06)

User reply at S8 blocker surfacing: `scp 本地 clone 上服务器`. Path A
chosen. Local 18 MB Test3R shallow clone (HEAD a2eb94b) uploaded via
scp; install proceeded on the server using local PyPI / conda channels
(pypi.org + conda.anaconda.org confirmed reachable; only github.com
blocked).

### scp upload outcome

```text
local source : E:\kykt\Dream\experiments\runs\cycle-015-critic-l3-pilot\Test3R
                (18 MB, --depth 1, HEAD a2eb94bc716df27521f053d417fcf6afa9870ff5)
remote target: KYKT-UI:/hdd3/kykt26/code/Test3R
command      : scp -r -o BatchMode=yes -o ConnectTimeout=12 Test3R
               KYKT-UI:/hdd3/kykt26/code/Test3R
result       : exit 0
verification : remote du -sh = 18 MB (matches);
               remote git log -1 = a2eb94b (HEAD matches);
               contents = assets/ croco/ datasets_preprocess/
                          demo.py dust3r/ eval/ LICENSE NOTICE
                          README.md requirements*.txt
```

### Test3R requirements + reachability scan

```text
Test3R/requirements.txt (11 lines, all PyPI; no `git+https://...`):
  torch, torchvision, roma, matplotlib, tqdm, opencv-python,
  scipy, einops, trimesh, tensorboard, pyglet<2,
  huggingface-hub[torch]>=0.22

requirements_optional.txt: scannetpp / visloc / pnp deps -> SKIP per
  cycle-015 scope.

pyproject.toml: none.

Reachability from server:
  pypi.org/simple/                        : HTTP/2 200
  conda.anaconda.org/pytorch/noarch/...   : HTTP/2 200
  github.com                              : (still blocked; expected)

Implication: no further github access required for the install path.
```

### conda env create + first PyTorch install (failed)

```text
conda create -n test3r python=3.11 cmake=3.14 -> success.
  env path: /home/kykt26/.conda/envs/test3r
  python   : 3.11.15
  pip      : at /home/kykt26/.conda/envs/test3r/bin/pip

conda install pytorch torchvision pytorch-cuda=12.1 -c pytorch -c nvidia
  -> install completed
  -> python -c "import torch" FAILED:

       ImportError:
       /home/kykt26/.conda/envs/test3r/lib/python3.11/site-packages/
       torch/lib/libtorch_cpu.so: undefined symbol: iJIT_NotifyEvent

Diagnosis: known MKL symbol mismatch. conda's `defaults` channel
shipped intel-openmp 2025.0.0 / mkl 2025.0.0 / numpy 2.4.4, but the
pytorch 2.5.1 conda build was compiled against an older MKL exposing
iJIT_NotifyEvent. Comparison with the working `dust3r` env on the same
server: dust3r uses pip-installed torch (`torch 2.5.1+cu121` from
PyPI wheels), NOT conda-installed pytorch. That's why dust3r works
where the new conda-style install fails.
```

### MKL fixup: switch to pip wheels (matches dust3r approach)

```text
Action: drop conda-installed pytorch family, pip install matching
versions from the pytorch wheel index. Within G_install scope (still
the same env, no privileged change, no re-gate).

  conda uninstall pytorch torchvision pytorch-cuda pytorch-mutex
                  torchtriton  -> success
  pip install --index-url https://download.pytorch.org/whl/cu121
              torch==2.5.1 torchvision==0.20.1
    -> Successfully installed torch-2.5.1+cu121
       torchvision-0.20.1+cu121 + nvidia-cu* runtimes

Final torch sanity (server-side):
  torch.__version__         : 2.5.1+cu121
  torch.cuda.is_available() : True
  torch.version.cuda        : 12.1
  torch.cuda.device_count() : 4
  torch.cuda.get_device_name(0) : NVIDIA TITAN RTX

Note: device_count = 4, NOT 2. The earlier nvidia-smi probe was
truncated by `head -15` and only showed devices 0 and 1; the box
actually has 4 TITAN RTX GPUs (24 GB each, ~96 GB total VRAM
budget).
```

### Test3R requirements install

```text
cd /hdd3/kykt26/code/Test3R
pip install -r requirements.txt
  -> Successfully installed 39 packages (11 direct deps +
     transitive). Notable:
       roma 1.5.6, einops 0.8.2, trimesh 4.12.2,
       opencv-python 4.13.0.92, scipy 1.17.1,
       matplotlib 3.10.9, tensorboard 2.20.0,
       huggingface-hub 1.13.0, pyglet 1.5.31

Final import sanity (server-side):
  python -c "import torch, torchvision, roma, einops, trimesh,
                    cv2, scipy, matplotlib, tqdm, tensorboard,
                    pyglet, huggingface_hub"
  -> all deps import OK
```

### Evidence label upgrades

```text
EXP-20260505-001 §b inferred numbers vs S8 measured:

  GPU class            inferred 24 GB  -> measured 4x TITAN RTX 24 GB
                                          (UPGRADED to `measured`)
  Disk per repo        inferred 0.5-2 GB
                                       -> measured 18 MB code-only
                                          (--depth 1; UPGRADED)
  Total clone disk     inferred 20-40 GB for 4 repos
                                       -> measured 28 MB local
                                          + 18 MB Test3R server
                                          = 46 MB total code
                                          (UPGRADED; checkpoints
                                          still pending at G_download)
  Env setup wall-clock inferred 1-2 hours
                                       -> measured ~25 min for
                                          conda env create + 2x
                                          pytorch install + Test3R
                                          requirements install
                                          (UPGRADED for `inferred`
                                          first-time on a clean box;
                                          this server box was
                                          warm-cached from prior
                                          backbone installs)
```

### Stop conditions checked at end of S8 resolution

```text
(a) URL 404                       : not triggered
(b) license blocks                : not triggered (CC BY-NC-SA 4.0
                                    + Apache 2.0 + the various
                                    Test3R deps' OSI licenses;
                                    research use OK)
(c) wall-clock blow-out           : not triggered (~25 min vs
                                    inferred 1-2 h; nowhere near
                                    10x stop)
(d) privileged system change      : not triggered (all changes are
                                    inside the test3r conda env at
                                    /home/kykt26/.conda/envs/test3r;
                                    no driver / system CUDA /
                                    kernel / PATH change)
(e) micro gate "no" or "redirect" : not triggered (user gave Go on
                                    G_install (revised); MKL fixup
                                    is within-scope environment
                                    repair, not a re-gate)
(f) output path drift             : not triggered (Test3R landed at
                                    /hdd3/kykt26/code/Test3R; cycle
                                    workspace at /hdd3/kykt26/dream-
                                    experiments/cycle-015 unchanged)
(g) no signed delta               : N/A (smoke not yet run)
```

### S8 outcome

S8 done. Test3R env (`test3r` conda env on server) is built and verified:

```text
- Test3R repo at /hdd3/kykt26/code/Test3R, HEAD a2eb94b
- conda env: test3r (Python 3.11.15)
- torch 2.5.1+cu121; CUDA 12.1; 4 GPUs visible
- 39 deps installed; all 11 Test3R direct deps import cleanly
```

Cycle 015 is now ready to surface G_download.

### What S9 will surface (G_download micro gate)

```text
- Test3R checkpoints: README + repo inspection still pending; agent
  will Grep README.md for "checkpoint" / "weight" / "huggingface" /
  "url" before composing the gate. Likely candidates:
    a) Naver / Hugging Face hosted (huggingface.co) - reachable
       per S8 reachability scan, given pypi.org and HF rely on
       different CDNs but huggingface.co is generally allowed
    b) Naver direct (download.europe.naverlabs.com or similar)
       - DUSt3R / MASt3R checkpoints sit on Naver Labs CDN
    c) GitHub releases - WOULD re-trigger the github.com block
       and require fallback (download local + scp up)
- DUSt3R + MASt3R checkpoints: already on server (per ServerConfig);
  REUSE; no download needed.
- Disk reservation: 1-5 GB inferred per checkpoint per
  EXP-20260505-001 §b; 3.6 TB free, abundant.

The agent will surface G_download with concrete URLs / sizes /
landing paths. Until user replies `Go`, NO download.
```

## S9 G_download resolution: degenerate (no download; reuse + patch)

User reply at S9 surface: `Patch launch.py 指向已有 ckpt`. Path A
chosen.

### Test3R weight-loading topology (read from server-side files)

Probes (read-only):

```text
huggingface.co reachability   : timeout (blocked from server)
download.europe.naverlabs.com : HTTP/1.1 200 OK
raw.githubusercontent.com     : redirects to github.com (blocked)

Test3R/README.md              : no checkpoint URL section
Test3R/eval/mv_recon/run.sh   : default model_name =
                                'DUSt3R_ViTLarge_BaseDecoder_512_dpt'
                                (note: same as DUSt3R production
                                checkpoint already on server)
Test3R/eval/mv_recon/launch.py:103
                              : weights_path = "naver/" + args.model_name
                                -> goes through huggingface_hub
                                   (HF id: naver/DUSt3R_ViTLarge_
                                            BaseDecoder_512_dpt)
Test3R/dust3r/model.py:100-108
                              : from_pretrained dual-mode:
                                if os.path.isfile(path):
                                    load_model(path)        <- offline
                                else:
                                    super().from_pretrained() <- HF
```

Key insight: Test3R is NOT a separate checkpoint; the official DUSt3R
checkpoint at `/hdd3/kykt26/models/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth`
(2.2 GB; pre-existing for the dust3r runner) IS Test3R's weight. The
"test-time training" dimension is a per-scene prompt update at inference
time, not a different pretrained file.

Conclusion: G_download is degenerate. No actual download is needed;
only a one-line code edit so Test3R loads the local file instead of
calling HF.

### Patch executed (Surgical Edits compliant)

```text
Backup       : cp eval/mv_recon/launch.py eval/mv_recon/launch.py.cycle015.bak
Patch (sed)  : line 103
  before     : weights_path = "naver/" + args.model_name
  after      : weights_path = "/hdd3/kykt26/models/DUSt3R_ViTLarge_
                              BaseDecoder_512_dpt.pth"
                              # cycle 015 patch: HF blocked; use local ckpt

Patch scope  : Test3R repo (uploaded by this cycle's S8 scp; not a
               KYKT production runner; not a Naver upstream source).
               Touches one file at one line. Backup retained for
               easy revert.
Why this is OK under DEC-005:
  - Within-scope environment repair, not a re-gate.
  - DEC-005 "no silent upstream patches" forbids patching
    Test3R / CTRL / DUSt3R / MASt3R sources without a fork note.
    The patch here is on the Test3R copy under
    /hdd3/kykt26/code/Test3R/, which is the cycle-015 working copy
    (the agent-uploaded clone), not an upstream PR / not a fork
    sent to Naver / not a change to KYKT runners. The fork-note
    requirement is satisfied by this cycle log section + the
    inline `# cycle 015 patch:` comment in the file itself.
  - DEC-005 "no KYKT navigation change" / "no KYKT runner change"
    not affected: /hdd3/kykt26/code/Test3R/ is not in
    /hdd3/kykt26/runners/ or /hdd3/kykt26/code/vision_runner/;
    KYKT's existing runner scripts and registry are untouched.
```

### End-to-end model-load verification

```text
ssh KYKT-UI -> conda activate test3r ->
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1
  python -c "from dust3r.model import AsymmetricCroCo3DStereo;
             m = AsymmetricCroCo3DStereo.from_pretrained(
                 '/hdd3/kykt26/models/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth')
             ..."

Result:
  loading /hdd3/kykt26/models/DUSt3R_ViTLarge_BaseDecoder_512_dpt.pth ...
  instantiating AsymmetricCroCo3DStereo(enc_depth=24, dec_depth=12,
    enc_embed_dim=1024, dec_embed_dim=768, enc_num_heads=16,
    dec_num_heads=12, pos_embed='RoPE100',
    patch_embed_cls='PatchEmbedDust3R', img_size=(512, 512),
    head_type='dpt', output_mode='pts3d',
    depth_mode=('exp', -inf, inf), conf_mode=('exp', 1, inf),
    landscape_only=False)
  use prompt? False
  <All keys matched successfully>
  OK loaded type: AsymmetricCroCo3DStereo
  params: 571.2M
  first param dtype: torch.float32
  cuda transfer test...
  OK on cuda:0

Two informational warnings (NOT blockers):
  (i)  torch.load weights_only=False FutureWarning - pre-existing
       pytorch 2.5+ deprecation noise; all dust3r-family code emits
       this; not a security risk for a Naver-published research
       checkpoint we already trust under the dust3r runner.
  (ii) "cannot find cuda-compiled version of RoPE2D, using a slow
       pytorch version instead" - the RoPE CUDA kernel build step
       was deliberately skipped per cycle-015 scope (perf-only).
       Slow path is correct; only matters for G_run wall-clock.
```

### Stop conditions checked at end of S9

```text
(a) URL 404                       : not triggered (no URL touched
                                    during the resolution; HF call
                                    avoided entirely)
(b) license blocks                : not triggered (CC BY-NC-SA 4.0;
                                    research use OK; the patch
                                    keeps the LICENSE intact)
(c) wall-clock blow-out           : not triggered (S9 was a one-line
                                    patch + 30-second model load test)
(d) privileged system change      : not triggered
(e) micro gate "no" or "redirect" : not triggered (user gave Go)
(f) output path drift             : not triggered (patch + backup
                                    landed inside /hdd3/kykt26/code/
                                    Test3R/eval/mv_recon/, the
                                    cycle-015 working copy)
(g) no signed delta               : N/A (smoke not yet run)
```

### Evidence label upgrades at S9

```text
- Test3R "checkpoint inventory" path was `inferred` (paper-derived)
  in EXP-20260505-001 §a. After S9, the route is `measured`:
  Test3R loads the same DUSt3R ViTLarge 512 dpt weight that
  is already on the server; no separate Test3R checkpoint exists
  in the public release at this HEAD (a2eb94b).
- Critic L3 prereq inventory item "ckpt access" for Test3R: cleared
  via reuse, NOT via download. EXP-001 §b inferred 1-5 GB checkpoint
  download per repo; for Test3R this is now `measured: 0 GB
  additional` (already on disk).
```

### S9 outcome

S9 done. Test3R can now run end-to-end on the server under the test3r
conda env, loading the 2.2 GB DUSt3R weight directly from disk. No
network call to huggingface.co or github.com is required for the
smoke-test path.

Cycle 015 is now ready to surface G_run.

### What S10 will surface (G_run micro gate)

```text
The G_run gate question must surface:

- Smoke goal (per DEC-005 + EXP-001):
    1 hard-case input pair (or triplet)
    -> DUSt3R pointmap A
    -> Test3R consistency objective signal S
    -> if S > theta_critic, Composer-backed reroute to MASt3R
    -> pointmap B
    -> consistency on B
    -> log delta (signed)

- Hard-case input candidate (proposed):
    Re-use one of the existing KYKT job inventories that the
    Critic L2 cards already reference - e.g. KYKT job 20260420-
    222729 (MASt3R static pair) or one of the cycle-009 /
    cycle-010 job inputs. The exact image pair will be selected
    from CRITIC-01 failure taxonomy (low-texture or symmetry
    case). Specifically, the two-frame pair input lives under
    /hdd3/kykt26/jobs/<job_id>/inputs/ on the server.

- Wrapper script (NEW; agent will draft, user reviews, then run):
    /hdd3/kykt26/dream-experiments/cycle-015/dream_critic_loop.py
    ~ 150-300 LOC; orchestrates the 6-step path above; one JSONL
    line per (input, S, reroute_decision, revised_output, delta).

- Resource budget:
    1 GPU (out of 4 TITAN RTX 24 GB); resolution 512 (fits 24 GB);
    expected wall-clock per case 5-20 min.

- NOT done at G_run:
    no full sweep across multiple regimes; no training; no
    storyboard promotion; no G2 closure claim; no KYKT runner
    change; no edits to dust3r/ or mast3r/ envs; no edits to
    KYKT runner scripts.

- Stop conditions (g) in particular: if smoke produces no signed
  delta (e.g. Test3R consistency signal is null, reroute
  unsuccessful, or revised output is unparseable), record as env
  smoke not L3 evidence pilot; do NOT silently upgrade CRITIC-01.

The agent will draft the wrapper as a markdown-only proposal
before any code is written, surface it as part of the G_run gate
question, and only execute after user `Go`.
```

## S10 hold: cycle 015 paused at S9 done by user choice (2026-05-06)

The agent surfaced a session-end summary plus a "next step" question
listing 4 options:

```text
A. Probe /hdd3/kykt26/jobs/ for hard-case candidates and return
   with concrete G_run options
B. git commit current markdown progress, hold cycle 015 mid-S10
C. Hold S10 (today's progress ends here)
D. User supplies design-choice answers immediately
```

User reply: `Hold S10（今日进度到此为止）`. Cycle 015 is paused at
S9 done; deliberately not closed. No surface attempt at G_run beyond
the design preview already in this log; no probe of /hdd3/kykt26/jobs/;
no draft of dream_critic_loop.py; no commit (user did not request).

State at pause (2026-05-06 end of session):

```text
Done sub-passes  : S1 .. S9 (all `done` in subtask board)
Pending         : S10 (G_run) - design previewed, not surfaced
                  S11 (G_log_use) - not started
Cycle status    : in_progress (per TASK_SNAPSHOT update protocol
                  item 3 - mid-task interrupt-style hold; user
                  choice, not blocker / not failure)

What is real on disk:
  local  E:\kykt\Dream\experiments\runs\cycle-015-critic-l3-pilot\
         (4 shallow clones; ~28 MB total; license sniff recorded)
  server /hdd3/kykt26/code/Test3R/         (18 MB; HEAD a2eb94b;
         launch.py:103 patched + .cycle015.bak backup)
         /hdd3/kykt26/dream-experiments/cycle-015/  (empty)
         /home/kykt26/.conda/envs/test3r/ (Python 3.11.15;
         torch 2.5.1+cu121; Test3R reqs all imported; verified
         loads 571.2M params on cuda:0)

What was NOT done:
  - No commit of any cycle-015 markdown work (TASK_SNAPSHOT,
    DEC-005, this cycle log, sync chain, F-002, memory). User
    can `git status` to see staged + unstaged state at resume.
  - No draft of dream_critic_loop.py wrapper.
  - No hard-case input selection.
  - No theta_critic value pinned.
  - No quality metric pinned.
  - No actual Critic smoke run.
  - No L3 evidence produced (CRITIC-01 evidence label still
    `inferred`; G2 still `inferred-with-real-inventory-anchor`,
    not closed).

What still needs answering at resume (the 5 G_run design choices):
  1. hard-case input source (KYKT job inventory vs. public
     benchmark hard-case)
  2. theta_critic value (paper-derived 0.05 vs. measure-then-
     pin)
  3. quality metric for delta (depth-error vs GT, vs. pointmap-
     internal consistency proxy)
  4. wrapper form (standalone Python script under
     /hdd3/kykt26/dream-experiments/cycle-015/ vs. KYKT-style
     ssh dispatched runner)
  5. resolution (512 default vs. 384-first-then-512)
```

Stop conditions checked at end of S10 hold:

```text
(a) URL 404                       : not triggered (S10 was
                                    surface-only; no URL touched)
(b) license blocks                : not triggered
(c) wall-clock blow-out           : not triggered
(d) privileged system change      : not triggered
(e) micro gate "no" or "redirect" : NOT triggered. User did NOT
                                    say "no" to G_run — they
                                    said "hold today, resume
                                    later". G_run gate has not
                                    been surfaced as a binding
                                    question yet (only as design
                                    preview). When user returns,
                                    surfacing the binding G_run
                                    gate is the resume action.
(f) output path drift             : not triggered
(g) no signed delta               : N/A (smoke not yet run)
```

Cycle 015 honest summary at pause:

```text
- Test3R env on server is real and verified (server-side measured).
- DUSt3R + MASt3R existing infrastructure unchanged.
- All 4 demo storyboards still `draft`.
- v2.1 contract unchanged.
- G2 / G6 / G7 unchanged.
- No L3 evidence produced.
- Two new lessons persisted for the next agent / session:
    F-002 in TASK_SNAPSHOT.md "Recent failure modes"
    feedback_kykt_server_topology.md in user-level memory
```

Resume action (when user returns):

```text
1. Re-read TASK_SNAPSHOT.md "If interrupted, resume from"
2. Surface the G_run binding gate with concrete options for
   each of the 5 design choices above. Recommended pre-surface
   read-only probe: inspect /hdd3/kykt26/jobs/ for hard-case
   candidates with available GT depth so options carry real
   anchors.
3. Do NOT execute the smoke without explicit user `Go` on G_run.
4. Do NOT skip the gate by drafting + running the wrapper as a
   single fait accompli; the gate exists per DEC-005.
```

End of S10 hold note.

## Discipline summary

```text
- Surgical Edits: only DEC-20260505-005 + this cycle log are NEW;
  S3 + S4 will be diff-only Edit calls on existing sync files.
- Honesty Override: every operational number is `inferred`; the
  authorization itself is `user-decided`; no L3 evidence claim
  produced.
- F-001 anti-32MB: TASK_SNAPSHOT.md updated FIRST in sync chain;
  large files in active context capped at <= 2; full-file Reads
  avoided on already-cited content.
- Hard rules: no reproduction / no checkpoint download / no training /
  no KYKT nav change / no frontend / no thesis finalization / no
  retiring of any non-finalist track happen during S1..S5. Each future
  step gated.
```
