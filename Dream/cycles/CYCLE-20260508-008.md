# Cycle 031: Memory v0.3 P0 local scaffold and ABL-memory-0 gate

Status: done

Date: 2026-05-08

Trigger: user authorized proceeding with "那就先进行后续操作吧" after the
cycle 030 P0 execution template and GitHub CLI discussion.

## Scope

Cycle 031 was bounded by
`decisions/DEC-20260508-007-cycle-031-p0-local-static-tensor-scaffold.md`.

Authorized:

- local prototype files under `experiments/prototypes/memory_v03_p0/`
- deterministic P0 fixtures
- oracle-bus tagging
- raw-label exclusion audit
- `ABL-memory-0` fixture/logging validity gate
- local JSON / JSONL / CSV / Markdown outputs

Not authorized:

- `Dream/code/` edits
- server integration
- model runtime imports
- checkpoint use
- training or fine-tuning
- frontend changes
- paper evidence promotion
- `ABL-memory-1..8` behavior claims
- `ABL-memory-9..11`

## Artifacts

New decision:

```text
decisions/DEC-20260508-007-cycle-031-p0-local-static-tensor-scaffold.md
```

New local prototype scaffold:

```text
experiments/prototypes/memory_v03_p0/README.md
experiments/prototypes/memory_v03_p0/requirements.txt
experiments/prototypes/memory_v03_p0/pyproject.toml
experiments/prototypes/memory_v03_p0/run_ablations.py
experiments/prototypes/memory_v03_p0/src/memory_v03_p0/
experiments/prototypes/memory_v03_p0/tests/test_abl_memory_0.py
```

Generated local outputs:

```text
experiments/prototypes/memory_v03_p0/outputs/fixtures_manifest.json
experiments/prototypes/memory_v03_p0/outputs/write_log.jsonl
experiments/prototypes/memory_v03_p0/outputs/metrics_abl_memory_0_8.csv
experiments/prototypes/memory_v03_p0/outputs/summary_go_no_go.md
experiments/prototypes/memory_v03_p0/outputs/evidence_boundary_update.md
```

## Result

`ABL-memory-0` passed.

```text
validity_checks_passed: 22/22
```

Validated locally:

- P0 tensor shape contract
- deterministic tensor and raw-label hashes
- oracle-bus field tags
- raw fixture label exclusion from variant inputs
- output serialization to JSON, JSONL, CSV, and Markdown

The CSV reserves rows for `ABL-memory-1..8` as `not_run_scope_deferred`.
Those rows are not results and do not imply implementation.

## Verification

Commands run:

```text
python run_ablations.py --output outputs
python -m pytest tests
python tests\test_abl_memory_0.py
```

Observed:

- CLI returned `{"abl_id": "ABL-memory-0", "status": "pass"}`.
- `python -m pytest tests` could not run because this Python environment
  does not have `pytest` installed.
- The same fixture and output checks were executed directly with
  `python tests\test_abl_memory_0.py`; exit code 0.

## Evidence Boundary

Cycle 031 supports only this claim:

```text
The local Memory v0.3 P0 fixture/logging substrate can be deterministically
generated, audited for raw-label leakage, serialized, and checked through
the ABL-memory-0 validity gate.
```

Still not validated:

- `ABL-memory-1..8` behavior
- spatial memory retrieval quality
- state-token recurrence quality
- hybrid bus-gated write benefit
- reconstruction quality
- checkpoint or real model behavior
- server integration
- paper-level empirical evidence

## Next Recommended Cycle

Cycle 032 should implement `ABL-memory-1` locally:

```text
vector AnchorBank baseline + retrieval/write metrics on the existing P0
fixtures, preserving oracle-bus and raw-label boundaries.
```

No server-side prototype or `Dream/code/` edit should happen before the
local ABL-memory-1 evidence boundary is written and accepted.
