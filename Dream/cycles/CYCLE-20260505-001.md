# CYCLE-20260505-001 Cycle 009 Case-Card Filling (Critic + Composer parallel)

Last updated: 2026-05-04 (cycle 009 case-card portfolio complete = 6 cards drafted + Critic-side 20260505 -> 20260504 ID drift cleaned up; S6 cross-spec contract usage audit recorded; S7 closeout written; guidance file sync chain run; only S8 user-facing surfacing remains)

Status: **closing** (S6 + S7 done; S8 user-facing report pending; once delivered the snapshot flips to idle and cycle 009 closes)

Phase: Phase 1.5 (Research Workflow Deployment) — cycle 009 stays inside Phase 1.5 per cycle 008 D1.

## Launch context

- launch authorization: `decisions/DEC-20260504-003-cycle-009-launch.md` (D1' = parallel, D2' = paper-derived, D3' = deferred-placeholder, D4' = go)
- launch package: `planning/CYCLE_009_LAUNCH_PACKAGE.md`
- prior cycle log: `cycles/CYCLE-20260504-001.md` (cycle 008.5 closeout + post-closeout inventory sync)

## Subtask board (active)

| ID | Subtask | Status | Canonical artifact |
|---|---|---|---|
| G1 | Record user `go` and four-decision values in DEC | done | `decisions/DEC-20260504-003-cycle-009-launch.md` |
| S1 | Open this cycle log; flip `TASK_SNAPSHOT.md` to `in_progress`; copy launch-package skeleton into snapshot subtask board | done | this file + `TASK_SNAPSHOT.md` |
| S2 | Draft `cases/CASE-20260504-CRITIC-01.md`: first Critic case card; cite `SPEC-20260503-001` + cross-spec contract CR-1 / CR-3 / CR-4 / CR-5 / CR-6 | done | `cases/CASE-20260504-CRITIC-01.md` |
| S3 | Draft `cases/CASE-20260504-CRITIC-02.md`: second Critic case card; Fast3R vs Spann3R route comparison; carries route_regret spread + CR-1 exercise; same contract anchors | done | `cases/CASE-20260504-CRITIC-02.md` |
| S4 | Draft `cases/CASE-20260504-CRITIC-03.md`: third Critic case card; MonST3R 48-frame per-frame detection-rate carrier; exercises CR-3 (cross-spec read of Memory latent_drift_proxy without gating A4) | done | `cases/CASE-20260504-CRITIC-03.md` |
| S5 | Draft `cases/CASE-20260505-COMPOSER-01..03.md`: three Composer case cards from paper-derived capability cards (D2' default); cite `SPEC-20260504-001` + CR-1 / CR-4; surfaces v1 -> v2 route_regret cost-axis candidate in card 03 | done | `cases/CASE-20260505-COMPOSER-01.md` + `02.md` + `03.md` |
| S6 | Cross-spec contract usage audit: verify CR-1..CR-6 instances recorded across the 6 drafted case cards; capture v1 -> v2 candidates | done | "Contract Usage Audit (S6)" section below |
| S7 | Cycle 009 closeout: write closeout section in this file; sync `RESEARCH_STATE.md` / `WORKFLOW_STATUS.md` / `INDEX.md` / `AGENT_MASTER_PROMPT.md` / `README.md`; run Guidance File Sync Rule chain starting from `TASK_SNAPSHOT.md` (per anti-F-001 rule 6) | done | "Closeout (S7)" section below + sync targets |
| S8 | Surface cycle-009 outputs to user: brief 3-section report (case cards drafted / contract gaps found / D-decisions surfaced for cycle 010); ask for cycle-010 launch decisions | pending | user-facing message |

## Anti-F-001 budgeting plan for this cycle

Each case card under S2..S5 will be treated as one anti-F-001 sub-pass:

1. one targeted spec slice (`Read` with `offset` + `limit`, NOT full file) per card
2. one targeted planning slice (`ACTION_TAXONOMY_AND_PROXY_METRICS.md` for A / P / F lookups; `ARCHITECTURE_MECHANISM_INTAKE.md` for M lookups) per card
3. cross-spec contract slice for the relevant CR rules (already in conversation context after S2; no need to re-read for S3..S5)
4. draft and write the card; do NOT re-Read prior case cards (cite their case_id, not their contents)
5. checkpoint commit between sub-passes when context budget feels tight
6. if a single Edit's old_string / new_string would exceed ~200 lines, split at section anchors

## Notes

- All case cards must respect cross-spec contract CR-6: record contract usage explicitly in the case-card body (which contract fields the card consumes, which it publishes).
- All case cards must carry an explicit `evidence_label` per the proxy_case_card.md template; no field labelled "demo-observed" or "code-observed" without a concrete pointer; default to `paper-proven` or `inferred` for cycle 009.
- No claim of measured performance is allowed in any case card (template "A case card must not claim measured performance" rule).
- D3' (TEACHER_AUDIENCE_PROFILE.md) remains placeholder; case cards are independent of this file per its own "Agent Behavior While This File Is Empty" section. S8 will repeat the D3' ask for cycle-010 decision packet.

## Contract Usage Audit (S6)

This section answers cycle 009 S6 by mapping every CR rule of `paradigm/CROSS_SPEC_SIGNAL_CONTRACT.md` v1 to its instance(s) across the 6 drafted case cards, then enumerating v1 -> v2 candidates surfaced by the portfolio. The audit is paper-derived end to end (D2'); no measured numbers are claimed.

### CR rule x case card matrix

Legend: `EX` = exercised (the rule fires and shapes the case-card decision); `CL` = closure (the producer half of a CR-1 / CR-4 hand-off lands in this card); `TH` = trivially honored (rule applies but the card does not put it under stress); `NA` = rule out of scope for this card's spec ownership.

| CR rule | CRITIC-01 | CRITIC-02 | CRITIC-03 | COMPOSER-01 | COMPOSER-02 | COMPOSER-03 |
|---|---|---|---|---|---|---|
| CR-1 (Critic A5 reroute requires Composer agreement) | TH (no reroute chosen; rerun_local_region) | EX (predicted Critic publish + Composer agree/veto loop, with veto fallback to conflict_unresolved) | TH (capability_match for alternatives predicted_low; reroute not chosen) | CL (Composer publishes capability_match spread = 0.09; tau_spread = 0.05 inferred; this is the producer side of CRITIC-02's CR-1 read) | TH (capability_match spreads 0.42 / 0.53, well above any plausible epsilon_tie) | TH (different model pair than the CRITIC cards; CR-1 closure for this regime would be a future Critic card on Fast3R-on-pair or MASt3R-SfM-on-many-view) |
| CR-2 (Permanence suppress_static_write binding on Memory) | NA | NA | NA | TH | TH | TH |
| CR-3 (Memory drift does not gate Critic verification) | TH (no latent_drift_proxy read) | TH | EX (Critic A5 reads latent_drift_proxy as cross-spec read to choose between rerun_local_region and open_anchor_budget; A4 still fires from in-scope signals; if Memory cards are not yet drafted the read returns a forward-reference null and the policy falls back to rerun_local_region) | NA | NA | NA |
| CR-4 (Composer route_recommendation does not bind Critic on capability_match ties) | TH | EX (loophole protection; if CR-1 returns Composer veto, CR-4 prevents the policy from then invoking a Composer route_recommendation override; fallback is conflict_unresolved, not Composer's preferred route) | TH | CL (Composer publishes route_recommendation but explicitly does NOT claim binding force) | TH (large spreads; CR-4 binding-prevention does not need to fire) | EX-v2 (cost_adjusted spread at alpha = 0.5 is exactly 0 on input 2; CR-4 prevents Critic from binding on this tie under the v2 framing; under v1 framing capability-only spread is 0.325, no tie, CR-4 dormant) |
| CR-5 (producer evidence label propagates) | EX | EX | EX | EX | EX | EX |
| CR-6 (cycle 009 case cards record contract usage) | EX | EX | EX | EX | EX | EX |

### Single-line interpretation per CR rule

- **CR-1**: closed cleanly on the cycle-009 paper-derived axis via the CRITIC-02 <-> COMPOSER-01 cross-pair. tau_spread = 0.05 is inferred, not measured; cycle 010 remains the place where tau_spread can move from inferred to demo-observed.
- **CR-2**: zero substantive coverage in cycle 009 by design (no Permanence card, no Memory card; cycle 008 D1 ordered Critic + Composer first). Recorded as a contract gap to be closed in cycle 010 when Memory + Permanence cards are drafted.
- **CR-3**: exercised once (CRITIC-03) under a forward-reference shape: the latent_drift_proxy read returns null until Memory cards exist. The forward-reference shape is the design (CR-3 explicitly does NOT gate A4); cycle 010 will replace the null with an actual Memory-published value once Memory's CASE-MEMORY-* cards exist.
- **CR-4**: covered on the Critic side as loophole protection (CRITIC-02), on the Composer side as non-binding declaration (COMPOSER-01), and meaningfully exercised only under the v2 cost-typed framing (COMPOSER-03). Under v1 alone, CR-4 has no real tie to resolve in the cycle-009 portfolio.
- **CR-5**: universally enforced. Every cross-spec signal value in the portfolio carries an explicit evidence_label with paper-proven, inferred, or forward-reference status; no implicit upgrades.
- **CR-6**: universally satisfied. Every card carries a "Cross-Spec Contract Usage (CR-6)" section.

### v1 -> v2 candidates surfaced this cycle

Recorded transparently per Discipline rule 5 Honesty Override. Adoption is **not** taken in this cycle; these are flagged for user / cycle-010 decision.

1. **Cost-typed route_regret axis** (origin: `cases/CASE-20260505-COMPOSER-03.md`).
   - v1 wording: route_regret is regime-typed.
   - v2 candidate wording: route_regret is regime-typed AND cost-typed.
   - Concrete artifact change: capability_match adds a `cost_normalized` axis; the recommendation under cost-asymmetric regimes can flip versus v1.
   - On the COMPOSER-03 input pair, v1 recommends MASt3R-SfM by capability-only spread of 0.325; v2 at alpha = 0.5 produces a spread of exactly 0 on input 2 (a CR-4 tie), so the recommendation flips to "no binding choice" and surfaces to Advisor.
   - Adoption preconditions for the user / cycle 010: confirm cost normalization formula; confirm alpha; confirm whether the v1 recommendation row is preserved as superseded (Discipline rule 5) rather than overwritten.

2. **Forward-reference null protocol for CR-3 reads** (origin: `cases/CASE-20260504-CRITIC-03.md`).
   - v1 wording: latent_drift_proxy is read read_only by Critic and does not gate A4.
   - v2 candidate wording: latent_drift_proxy reads must declare their behavior when the producer card has not yet been authored (forward-reference null + fallback to rerun_local_region was the cycle-009 default; ratify or revise in v2).
   - Concrete artifact change: a new "forward-reference" field in the contract's per-signal rows; otherwise no rule-change.
   - This is a low-stakes documentation-grade promotion; it can be folded into v1.x without producing v2 if the user prefers.

### Contract gaps recorded for cycle 010

- **G1 (CR-2 zero coverage)**: no Memory or Permanence case card exists in cycle 009, so the suppress_static_write binding is never put under stress. Cycle 010 draft of `cases/CASE-20260504-MEMORY-*` + `cases/CASE-20260504-PERMANENCE-*` is the closure path.
- **G2 (tau_spread inferred)**: CR-1 closure relies on tau_spread = 0.05; this constant has no demo-observed grounding yet. Cycle 010 (or whatever cycle first lands measured route_regret on a real input) is the place to upgrade tau_spread from inferred to demo-observed.
- **G3 (CR-4 v1 dormancy)**: under v1 alone, CR-4 has no live tie to resolve in the portfolio. If v2 is rejected, CR-4 stays decoratively present without exercise; if v2 is adopted, CR-4 becomes a routine arbiter in cost-asymmetric regimes.

These gaps are recorded as gaps, not papered over; they enter cycle 010's launch package automatically.

## Closeout (S7)

### What this cycle produced

```text
6 case cards drafted under D1' parallel + D2' paper-derived:
  - cases/CASE-20260504-CRITIC-01.md   (static pair; A5 = rerun_local_region)
  - cases/CASE-20260504-CRITIC-02.md   (Fast3R vs Spann3R; CR-1 reroute_model + Composer agree/veto loop)
  - cases/CASE-20260504-CRITIC-03.md   (MonST3R 48-frame; CR-3 read of Memory latent_drift_proxy)
  - cases/CASE-20260505-COMPOSER-01.md (static-collection regime; CR-1 closure paired with CRITIC-02)
  - cases/CASE-20260505-COMPOSER-02.md (regime-typed route_regret central thesis card)
  - cases/CASE-20260505-COMPOSER-03.md (Fast3R vs MASt3R-SfM; v1 -> v2 cost-typed route_regret candidate)
1 mid-cycle ID-drift cleanup sub-pass:
  - Critic-side IDs corrected from 20260505-CRITIC-* (cycle log file
    date) to 20260504-CRITIC-* (spec-authoritative date per
    SPEC-20260503-001 line 208; D2' paper-derived cards inherit the
    spec-reserved date, not the cycle-log file date).
  - 9 files touched in one commit; no semantic content change.
1 contract usage audit (this file's "Contract Usage Audit (S6)" section).
3 v1 -> v2 candidates surfaced for cycle-010 user decision (cost-typed
  route_regret + forward-reference null protocol; the forward-reference
  promotion can fold into v1.x without producing v2 if the user
  prefers).
3 contract gaps recorded for cycle-010 closure (G1 / G2 / G3 above).
```

### Status of locked / open / blocked items

- D1' (parallel ordering): honored; 3 Critic + 3 Composer cards drafted in parallel.
- D2' (paper-derived): honored; no KYKT-job-derived capability cards in this cycle.
- D3' (TEACHER_AUDIENCE_PROFILE.md): partially populated 2026-05-04 by user input; three sub-fields remain empty by user choice; agent did not invent priorities. Cycle 009 case cards are independent of this file per its own "Agent Behavior While This File Is Empty" section.
- D4' (cycle 009 launch authorization): closed; cycle 009 ran to completion.
- D3 (first demo target): still deferred per `decisions/DEC-20260504-002-no-all-in-on-single-finalist.md`; the cycle 009 portfolio is the input to a future D3 decision but does not force one.
- D5..D8 (paper writing, reproduction, KYKT navigation, frontend): unchanged. All blocked on user approval per `AGENT_MASTER_PROMPT.md` section 6.

### Surgical Edits compliance

- No retro-renumbering of pre-existing IDs.
- No deletion of historical content.
- Cross-spec contract v1 itself is unchanged this cycle; v1 -> v2 candidates are recorded above as candidates only.
- `cases/CASE-20260505-CRITIC-*` IDs corrected to `cases/CASE-20260504-CRITIC-*` IDs in a dedicated commit because the original drift was a creation-time labelling error, not a "renumbering" of a previously valid ID. Honesty Override compliance: the commit message records the date drift explicitly so the history is preserved.
- F-001 working rules honored: no full-file Reads of large state files in this closeout pass; case-card contract usage was extracted via narrow Read windows (offset / limit), not full reads.

### Guidance File Sync Rule chain run

Per anti-F-001 rule 6, `TASK_SNAPSHOT.md` was synced first so an interrupt mid-pass would still leave a valid resume pointer.

| File | Change | Trace |
|---|---|---|
| TASK_SNAPSHOT.md | header + subtask board (S6/S7 done; S8 active) + If-interrupted-resume-from + Last completed task pass | Sync Rule trigger; anti-F-001 rule 6 puts this first |
| cycles/CYCLE-20260505-001.md | this file: header + subtask board flips + Contract Usage Audit (S6) section + Closeout (S7) section | Cycle log is the canonical S6/S7 artifact |
| RESEARCH_STATE.md | "Cycle 009 Case-Card Filling Closeout (CYCLE-20260505-001)" section appended at end + last-updated stamp | Sync Rule trigger; substantive cycle progress |
| WORKFLOW_STATUS.md | Cycle logs row pointer + Cross-spec contract row + Geometry Critic + 3R Composer rows updated to "L2 case cards drafted, paper-derived"; Recommended Next User Decision rewritten to cycle-010 launch + contract-gap closure + v1 -> v2 candidate decisions; last-updated stamp | Sync Rule trigger |
| INDEX.md | new `cases/` subsection rows for the 6 case cards + last-updated stamp | new artifacts |
| AGENT_MASTER_PROMPT.md | last-updated stamp only; no protocol change (case cards are not a new mandatory-load category; existing `cases/` mention covers them) | Sync Rule trigger |
| README.md | last-updated stamp only | Sync Rule trigger |

### Cycle 010 launch package preconditions (carried into S8)

Cycle 010 launch will need user decisions on:

1. **v2 cost-typed route_regret** (adopt, defer, or fold into v1.x).
2. **Cycle 010 ordering** (Memory + Permanence cards in parallel? Or Memory first because suppress_static_write binding originates Permanence-side?).
3. **D3 demo target** (still deferrable; surfaced for the user to lift if desired).
4. **TEACHER_AUDIENCE_PROFILE.md remaining sub-fields** (Prior Expectations / Demo Precedent / Previously Criticized still empty; user can populate or keep empty).

These four items form the body of the S8 user-facing message.
