# Cycle 033: Dream3R v0.3 architecture advancement on server (W1-W16)

Status: done

Date: 2026-05-10 (retroactive cycle log written 2026-05-13; original work executed 2026-05-09 through 2026-05-10 on server `/hdd3/kykt26/code/dream3r/`)

Trigger: implicit continuation from cycle 032 close. No formal DEC was written for cycle 033; authorization was given in active conversation per AGENT_MASTER_PROMPT section 6 carve-outs (server-side editing on already-deployed v0.3 code, no new training, no new checkpoint download beyond DINOv2 hub auto-cache).

Canonical plan artifact: [code/dream3r/CYCLE_033_PLAN.md](../code/dream3r/CYCLE_033_PLAN.md).

## Scope

Push the v0.3 codebase from "scaffold with stubs" to "every architectural pillar produces real computation". Sixteen workstreams W1-W16 grouped in 3 phases.

Authorized:

- Server-side edits to `/hdd3/kykt26/code/dream3r/` and local mirror under `Dream/code/dream3r/`
- DINOv2 hub auto-download for the frozen backbone (~350 MB)
- Loading existing server-side checkpoints for MASt3R / Fast3R / Spann3R adapters from already-deployed runner trees
- Sequence-level synthetic training on bus/state/loss/metric paths

Not authorized:

- Real-data training or fine-tuning
- New external dependency installs (`mamba_ssm`, `gsplat`, `omegaconf`)
- Frontend or KYKT navigation changes
- Paper claim promotion beyond "integration verified on synthetic data"

## Workstreams delivered

| ID | Workstream | Result |
|---|---|---|
| W1 | Bus temporal semantics (`read_previous`) | done; cross-tick CR-* contracts now read real signals |
| W2 | CR-3 retrieval policy + logged biases | done; effective top-k + permanence bias now wired |
| W3 | Spatial payload in AnchorBank (3D points, source pose, patch ids) | done |
| W4 | Real expert adapter integration | MASt3R + Spann3R load real checkpoints and dispatch through ComposerRouter; Fast3R checkpoint path present but blocked on missing `omegaconf` in `dream3r` conda env; CUT3R / MoGe-2 / DepthAnything / Test3R remain deterministic fallback |
| W5 | Sequence-level training harness (multi-window unroll + truncated BPTT) | done |
| W6 | Permanence hardening (per-slot `dynamic_ratio`, Hungarian-style slot matching, per-slot suppress) | done |
| W7 | Critic repair loop (actions 0/1/2 consumed downstream; 3/4/5 stubbed) | done |
| W8 | NSA refinement (top-2 gating + retrieval log) | partial; FlashAttention integration deferred |
| W9 | Loss advancement (geometric_consistency / retrieval_quality / routing_entropy / state_drift_regularization) | done |
| W10 | Evaluation metrics (AbsRel / SqRel / RMSE / Chamfer / F-score + architectural metrics) | done |
| W11 | Frozen DINOv2 backbone in Perceiver | done; verified by monkeypatched `torch.hub.load` without downloading weights |
| W12 | 3D position embeddings + spatial retrieval modes | done; `latent_only` / `latent_plus_3d` / `3d_only` |
| W13 | Active/stable state decoupling + promote/recall | done |
| W14 | SSR Grassmannian drift regularizer | done; plug-and-play; `grassmannian_strength=0.1` default |
| W15 | Critic geometric consistency signals (Sampson + covisibility + depth + confidence) | done |
| W16 | ISA per-slot reference frames + pose-aware slot matching | done |

W17 (Mamba hybrid) + W18 (3DGS head) intentionally deferred to cycle 034 / later cycles.

## Verification (all server-side via ssh)

```text
cd /hdd3/kykt26/code/dream3r
CUDA_VISIBLE_DEVICES=0 python -m dream3r.smoke_test            # 9/9 pass
python -m dream3r.tests.test_*                                  # full suite pass
python -m dream3r.tests.test_dinov2_backbone                    # pass (monkeypatched hub)
python -m dream3r.tests.test_3d_retrieval                       # pass
python -m dream3r.tests.test_active_stable                      # pass
python -m dream3r.tests.test_drift_regularizer                  # pass
python -m dream3r.tests.test_geometric_critic                   # pass
python -m dream3r.tests.test_isa_slots                          # pass
python -m dream3r.tests.test_permanence_v2                      # pass
python -m dream3r.tests.test_critic_loop                        # pass
python -m dream3r.tests.test_cr3_policy                         # pass
python -m dream3r.tests.test_sequence_training                  # pass
python -m dream3r.tests.test_loss_advancement                   # pass
python -m dream3r.tests.test_training_convergence               # pass
python -m dream3r.tests.test_evaluate_metrics                   # pass
DREAM3R_RUN_MAST3R_INTEGRATION=1   python -m dream3r.tests.test_mast3r_integration   # pass (loads real ckpt)
DREAM3R_RUN_SPANN3R_INTEGRATION=1  python -m dream3r.tests.test_spann3r_integration  # pass (loads real ckpt)
DREAM3R_RUN_FAST3R_INTEGRATION=1   python -m dream3r.tests.test_fast3r_integration   # blocked: missing omegaconf
```

## Evidence Boundary

Cycle 033 supports only the following claims:

```text
The v0.3 architecture integrates on synthetic data and on real-checkpoint MASt3R + Spann3R dispatch paths. Every architectural pillar in W1-W16 produces non-stub computation under unit and smoke tests on the server.
```

Still NOT validated:

- Real-data reconstruction quality
- SOTA comparison numbers
- Critic threshold calibration on real geometry distributions
- Expert routing quality with all 7 experts loaded
- Long-sequence (>10 window) degradation curves
- Training convergence on real datasets

## Next Recommended Cycle

Cycle 034: stabilize, sync discipline, and run first real-data smoke on KITTI using already-deployed data at `/hdd3/kykt26/data/kitti_rectified/`. Mamba hybrid recurrence and GaussianHead tensor contract may land alongside if no new packages are needed.
