# DEC-20260505-004: cycle 014 launch

date: 2026-05-05

status: accepted

decision_scope:

```text
Launch cycle 014 as a Phase 2 convergence and execution-selection cycle.
This is markdown-only research planning. It does not authorize L3
execution, clone, download, install, run, checkpoint access, KYKT runner
access, frontend work, storyboard showing, or final thesis selection.
```

## Trigger

User said:

```text
继续
```

This followed the agent's recommendation to continue with:

1. paper blueprint refinement,
2. VGGT Composer capability-card gap handling,
3. L3 pilot downselect.

## Decision

Cycle 014 will execute three content sub-passes:

```text
S2 paper blueprint:
  Create a Phase 2 paper blueprint that turns the current related-work
  prose draft into a claim-safe paper structure. The blueprint may
  specify title, problem statement, claim ladder, method skeleton,
  evidence table, missing experiments, and write order. It may not claim
  paper readiness.

S3 VGGT Composer gap:
  Add a per-card Composer addendum for the VGGT capability-card gap
  surfaced in cycle 013. This may incorporate VGGT, MapAnything, and the
  DUSt3R / MASt3R / VGGT MVS evaluation as `paper-proven` or `inferred`
  comparators according to source labels. This does not revise the v2.1
  cross-spec contract, because the existing `capability_card` schema can
  hold new model rows.

S4 L3 pilot downselect:
  Compare the four existing L3 prerequisite briefs and recommend the
  first L3 pilot target. The output must preserve all execution gates and
  must not run anything.
```

## Rationale

Cycle 013 produced enough supporting material to move from "research
mining" into "execution selection", but not enough measured evidence to
select a final thesis or show a demo.

The immediate bottleneck is not another source-mining pass. It is
choosing which evidence gap to close first while keeping the paper story
coherent:

- Critic has the shortest D3 demo path and clearest teacher-facing
  surprise hook.
- Composer has the most direct route to G2 closure, but the measured
  route-regret sweep is heavier and VGGT was absent from the existing
  capability-card portfolio.
- Memory and Permanence remain important, but their L3 smoke paths have
  higher dependency and data complexity.

## Locked exclusions

Cycle 014 explicitly excludes:

- cloning any repo,
- downloading checkpoints,
- installing environments,
- running model inference or smoke tests,
- reading KYKT runner logs,
- changing KYKT app navigation,
- direct frontend implementation,
- promoting storyboards beyond `draft`,
- declaring teacher-demo readiness,
- final thesis selection,
- retiring Cross-Modal or Active Perception tracks,
- changing `paradigm/CROSS_SPEC_SIGNAL_CONTRACT.md` unless a concrete
  contradiction is found during audit.

## Acceptance criteria

Cycle 014 can close only if:

1. A paper blueprint artifact exists and separates current L2-supported
   claims from L3-required claims.
2. A VGGT Composer addendum exists and states whether the gap is per-card
   or contract-level. Expected outcome: per-card, no v2.2 revision.
3. An L3 pilot downselect artifact exists and recommends one first pilot
   plus one backup, with evidence labels and execution gates intact.
4. The cycle log records an audit for overclaiming, contract drift, and
   unauthorized execution.
5. The guidance sync chain is run with `TASK_SNAPSHOT.md` first.

## Evidence status

This decision is `inferred` from existing Dream artifacts:

- `cycles/CYCLE-20260505-004.md`
- `experiments/EXP-20260505-001..004-l3-prerequisites-*.md`
- `literature/PAPER_RELATED_WORK_SKELETON.md`
- `literature/CRITICAL_NOTES.md`
- `paradigm/CROSS_SPEC_SIGNAL_CONTRACT.md` v2.1

It records user authorization to proceed with markdown convergence, not
authorization to execute L3.
