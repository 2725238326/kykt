# CASE-20260504-CRITIC-01

First Critic L2 case card per `specs/SPEC-20260503-001-geometry-critic.md` line 208 reservation. Paper-derived per cycle 009 D2' (`decisions/DEC-20260504-003-cycle-009-launch.md`). No measurement claimed; no model invocation; no KYKT job artifact opened. All numeric ranges below are paper-derived or inferred and labeled accordingly per Cross-Spec Signal Contract rule CR-5.

## Identity

case_id: CASE-20260504-CRITIC-01

proxy_id: P1

scenario_name: MASt3R static pair, hard-case verification on KYKT job 20260420-222729

date: 2026-05-04

linked_failure_modes: F3 | F6

linked_actions: A4 | A5

linked_branches: Geometry_Critic

linked_research_units: RU-003 | RU-011

linked_sources: Test3R | MASt3R-SfM | CTRL (see `registry/source_registry.md` for canonical IDs)

## Input Artifact

source_type: paper_example

artifact_pointer: `model_uploads/20260420-222729/` (MASt3R static pair: matches.png + .ply outputs; per `specs/SPEC-20260503-001-geometry-critic.md` line 439)

evidence_label: paper-proven (for the MASt3R output as upstream artifact and for the Test3R / MASt3R-SfM signal definitions reused below); inferred (for every Dream Critic A4 -> A5 prediction in this card)

input_summary:

```text
A KYKT-side static pair job run with MASt3R. Inputs are two RGB frames that
do not share a contiguous video timeline, so pose_novelty in the multi-view
temporal sense is not_applicable. MASt3R produces matches.png (sparse
correspondence visualisation) and a point cloud .ply. Critic treats the
pair as a single input window. The static-pair regime is the most adversarial
weak-pressure case for the Dream Critic differentiator: in-family critics
(Test3R) need cross-time signal and MASt3R-SfM needs more than two views, so
A5 reroute_model has limited upside here. This is the cheapest case to
surface the spec's weakest_pressure_point ("orphan critic report").
```

## Evidence Signals

Recorded values are predicted Critic readings under D2' paper-derived analysis. Numeric values are illustrative scales chosen to test the action policy, not measurements. Signals not used by Critic per `specs/SPEC-20260503-001-geometry-critic.md` lines 171-182 are marked `not_applicable_to_critic` rather than left blank, to make scope explicit.

pose_novelty: not_applicable (static pair; no temporal pose sequence)

view_overlap: predicted_low (paper-derived: MASt3R degrades on narrow-baseline / low-overlap pairs; this is the regime where the case is meaningful)

reprojection_residual: predicted_high on the conflict subset (paper-proven Test3R / MASt3R-SfM signal type; Dream re-uses the signal definition without re-deriving it)

pointmap_conflict: predicted_high (low view_overlap drives pointmap divergence between matched correspondences; paper-proven in MASt3R-SfM failure-mode discussion)

confidence_drop: predicted_present (MASt3R native confidence map drops on low-overlap regions; paper-observed)

latent_drift_proxy: not_applicable_to_critic (Memory spec scope per spec line 182)

dynamic_ratio: not_applicable_to_critic (Permanence spec scope per spec line 173)

optical_flow_conflict: not_applicable_to_critic (Permanence spec scope per spec line 174)

object_track_stability: not_applicable_to_critic (Permanence spec scope per spec line 175)

loop_candidate_score: not_applicable_to_critic (Memory spec scope per spec line 176)

anchor_importance: not_applicable_to_critic (Memory spec scope per spec line 177)

cache_pressure: not_applicable_to_critic (Memory spec scope per spec line 178)

external_memory_overlap: not_applicable_to_critic (Memory spec scope per spec line 179)

prior_rgb_conflict: read_only_for_critic (Cross-Modal spec ownership per spec line 169; Critic reads only and may trigger A5 request_prior)

blur_or_low_light_score: not_applicable_to_critic (Cross-Modal spec scope per spec line 180)

uncertainty_area: not_applicable_to_critic (Active Perception scope per spec line 181)

model_capability_match: predicted_marginal for MASt3R on this regime; predicted_low for MonST3R (dynamic-scene specialist) and Spann3R (streaming) on static pair; predicted_low for Fast3R on this specific regime per its design target. Result: A5 reroute_model has limited upside in this case.

## Comparator Policies

| Policy | Action chosen | Predicted proxy P1 value | Notes |
| --- | --- | --- | --- |
| comparator_1 single-pass MASt3R (no critic) | emit output verbatim | 0 detection rate | baseline; no mechanism to detect own conflict |
| comparator_2 Test3R-style verify-then-report | A4 only; report conflict score; no A5 attached | paper-proven >0 on cross-time regimes; inferred near-zero on static pair (out-of-regime) | the weak-pressure case for in-family critic; cannot distinguish itself from comparator_1 here |
| comparator_3 MASt3R-SfM | full SfM refinement | paper-proven detection at SfM stage | heavyweight; not viable as per-window critic budget |
| dream_policy A4 -> A5 closed loop | A4 fires on high pointmap_conflict + confidence_drop subset; A5 chooses rerun_local_region (narrow window to high-confidence match cluster); A5 reroute_model NOT chosen since model_capability_match for alternatives is low; A5 open_anchor_budget NOT chosen (no long context in static pair); A5 request_prior NOT chosen (no Cross-Modal spec active yet) | predicted detection >0 with explicit recommended action attached; route_regret_estimate small (no model switch) | the spec's weakest_pressure_point ("orphan critic report") is avoided here only if A5 rerun_local_region attaches |

## Predicted Proxy Outcome

primary_metric: P1 conflict detection (per `specs/SPEC-20260503-001-geometry-critic.md` line 201)

unit_of_measurement: detection_rate over labeled conflict windows in [0, 1]; false_alarm_rate over labeled non-conflict windows in [0, 1]

threshold_for_useful_signal: detection_rate >= 0.6 AND false_alarm_rate <= 0.5 (per spec acceptance_threshold lines 217-225)

predicted_dream_value: detection_rate inferred in [0.6, 0.9] from MASt3R + Test3R combined coverage on this regime (paper-derived range, not measured); false_alarm_rate is theta_conflict-dependent

predicted_best_comparator_value: comparator_2 Test3R-style detection_rate inferred near 0 on static pair (out-of-regime); comparator_3 MASt3R-SfM detection_rate paper-proven near upper range but at much higher cost

expected_gap_direction: dream_better against comparator_2 in this regime (in-family critic cannot fire); ambiguous against comparator_3 absent cost normalisation; unconditional dream_better against comparator_1

decision_signal_meaning:

```text
If on the labeled windows of this case the dream_policy fails to attach a
concrete A5 sub-action (i.e. emits an orphan conflict report), the spec's
weakest_pressure_point fires and the Critic spec retires per its
fail_fast_threshold. This case is the cheapest place to surface that risk.

Secondary proxy P5 route_regret carries little weight here because A5
reroute_model is not the chosen sub-action; route_regret discrimination is
the explicit job of CASE-20260504-CRITIC-02 (Fast3R vs Spann3R route).
```

## Cross-Spec Contract Usage (CR-6)

Recorded per Cross-Spec Signal Contract rule CR-6 (`paradigm/CROSS_SPEC_SIGNAL_CONTRACT.md` line 149).

- CR-1 (Critic A5 reroute_model requires Composer agreement on capability_match spread): not exercised in this case. A5 reroute_model is NOT the chosen sub-action because model_capability_match for alternatives is low. CR-1 will be exercised in CASE-20260504-CRITIC-02 instead.
- CR-3 (Memory drift signal does not gate Critic verification): trivially honored. No latent_drift_proxy input is read; Critic A4 fires independently.
- CR-4 (Composer route_recommendation does not bind Critic on capability_match ties): trivially honored. No tie exists in this regime; alternatives are predicted_low, not tied with predicted_marginal.
- CR-5 (All cross-spec signals carry producer evidence label): honored. Every signal value above carries an explicit evidence_label per `specs/SPEC-20260503-001-geometry-critic.md` lines 376-396 conventions.

## Writing Value

related_work_section: critic

figure_or_taxonomy: two-panel timeline (per `specs/SPEC-20260503-001-geometry-critic.md` lines 252-257) — Panel A original MASt3R matches.png + .ply; Panel B critic overlay with red-zone conflict subset and action label "rerun region (narrowed match cluster)" plus a route-regret bar showing why reroute_model was not chosen. This card supplies the static-pair entry of that figure.

novelty_claim_supported:

```text
"Cross-model A4 -> A5 closed loop" claim is supported in the negative
direction by this card: the static-pair regime shows that A5 reroute_model
is not always available, and the policy correctly falls back to
rerun_local_region. This demonstrates the policy is not "always reroute" -
which is precisely the differentiator the spec needs to defend against
the read "Dream Critic = relabeled Test3R + always-reroute heuristic".
```

## Risk And Boundaries

fail_fast_condition:

```text
If the Dream policy on this case predicts the same action as comparator_2
(emit conflict score, no A5 attachment), the orphan-critic risk fires and
the Critic spec must be retired per its fail_fast_threshold. This is the
single most informative failure signal among the three Critic cards.
```

reproduction_required: no

training_required: no

frontend_change_required: no

approval_required: no

caveats:

```text
- Every predicted value above is paper-derived or inferred. No KYKT job
  artifact has been opened for this card and no model has been invoked.
  This is consistent with cycle 009 D2' (paper-derived only). When the
  card is later upgraded with manual conflict labels (still no model
  invocation), the labels become code-observed and supersede the
  inferred predictions one signal at a time.
- Static-pair regime is a deliberate adversarial choice for the Dream
  differentiator. Choosing an "easy" multi-view case here would let the
  spec pass without testing the orphan-critic risk.
- Cross-spec contract CR-1 is intentionally not exercised here so that
  CASE-20260504-CRITIC-02 (Fast3R vs Spann3R) carries the load on that
  rule. Splitting CR exercise across cards is consistent with the
  acceptance_threshold's requirement that "route_regret has nonzero spread"
  (`specs/SPEC-20260503-001-geometry-critic.md` line 224) - that spread
  is the job of card 02, not card 01.
```

## Next Action

next_action: add_comparator

linked_next_artifact: `cases/CASE-20260504-CRITIC-02.md` (Fast3R vs Spann3R route comparison; the regime where A5 reroute_model is the chosen sub-action and CR-1 is exercised)
