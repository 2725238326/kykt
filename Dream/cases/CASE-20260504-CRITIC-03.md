# CASE-20260504-CRITIC-03

Third Critic L2 case card per `specs/SPEC-20260503-001-geometry-critic.md` line 210 reservation. Paper-derived per cycle 009 D2' (`decisions/DEC-20260504-003-cycle-009-launch.md`). No measurement claimed; no model invocation; no KYKT job artifact opened. All numeric ranges below are paper-derived or inferred and labeled accordingly per Cross-Spec Signal Contract rule CR-5.

This card is the explicit per-window detection-rate carrier for the Critic spec acceptance_threshold (`specs/SPEC-20260503-001-geometry-critic.md` lines 217-225 "conflict_detection >= 0.6 over labeled conflict windows AND false_alarm_rate <= 0.5 over labeled non-conflict windows"). It is also the only Critic card on a dynamic-scene regime, where the in-family Test3R-style comparator is at its native strength; the Dream differentiator therefore shifts from detection rate (where the gap is narrow) to A5 sub-action attachment (where the gap is the spec's whole point per `specs/SPEC-20260503-001-geometry-critic.md` lines 86-90).

## Identity

case_id: CASE-20260504-CRITIC-03

proxy_id: P1 (primary; complements card 01 which carries the static-pair adversarial regime under the same proxy)

scenario_name: MonST3R 48-frame per-frame conflict labels on KYKT job 20260420-222928

date: 2026-05-04

linked_failure_modes: F3 | F1 (long-context drift potentially active across 48 frames)

linked_actions: A4 | A5

linked_branches: Geometry_Critic

linked_research_units: RU-003 | RU-011

linked_sources: MonST3R | Test3R | TTT3R | CTRL (see `registry/source_registry.md` for canonical IDs)

## Input Artifact

source_type: paper_example

artifact_pointer:

```text
model_uploads/20260420-222928/  (MonST3R 48-frame job; 96 dynamic masks
per spec line 363 - approximately 2 mask outputs per frame on average,
or 2 mask versions across the sequence)
Per D2' the artifact is not opened for this card; the 48-frame +
96-dynamic-mask shape is taken from the spec's risk discussion line 363.
```

evidence_label: paper-proven (for MonST3R dynamic-scene capability + Test3R / TTT3R signal definitions); inferred (for every Dream Critic A4 -> A5 prediction below, including detection-rate prediction and sub-action attachment per window)

input_summary:

```text
A 48-frame sequence reconstructed by MonST3R, MonST3R's native dynamic-
scene regime. Per-frame conflict labels are the unit of analysis: each
frame is a candidate window for A4. The 96 dynamic masks (per spec
line 363) make this the highest-likelihood conflict-bearing job among
the four reserved KYKT jobs - which is why the spec deliberately picks
it for the per-frame detection card. Critically, this is also the
regime where in-family Test3R / TTT3R critics fire natively, so the
Dream differentiator narrows on raw detection rate and widens on A5
sub-action attachment: same conflict, different next step.
```

## Evidence Signals

Recorded values are predicted Critic readings under D2' paper-derived analysis. Numeric scales are illustrative.

pose_novelty: predicted_present_per_frame (48-frame sequence with dynamic content; pose vector evolves and re-evolves)

view_overlap: predicted_high overall, predicted_low on dynamic-occluded subwindows (paper-derived: MonST3R's own dynamic masks themselves indicate low-overlap subwindows)

reprojection_residual: predicted_higher on dynamic-mask frames than on static-only frames (paper-proven mechanism; per-frame magnitude inferred)

pointmap_conflict: predicted_present per dynamic mask vs static-only voxel intersection (paper-derived MonST3R dynamic-mask semantics)

confidence_drop: predicted_concentrated on dynamic regions (paper-observed: MonST3R confidence map drops where the dynamic mask is positive)

latent_drift_proxy: read_only_for_critic (Memory spec ownership per spec line 182). Cross-spec read: Critic A5 may consult this signal to decide between rerun_local_region (no drift) and open_anchor_budget (drift detected). Per CR-3, this read does NOT gate Critic A4 verification - A4 fires from in-scope signals only.

dynamic_ratio: not_applicable_to_critic (Permanence spec scope per spec line 173); however Permanence's published dynamic_ratio could be cross-read in a future cycle to validate Critic's predicted_concentrated label

optical_flow_conflict: not_applicable_to_critic

object_track_stability: not_applicable_to_critic

loop_candidate_score: not_applicable_to_critic

anchor_importance: not_applicable_to_critic

cache_pressure: not_applicable_to_critic

external_memory_overlap: not_applicable_to_critic

prior_rgb_conflict: read_only_for_critic; not_active in this case (no Cross-Modal prior available for these frames)

blur_or_low_light_score: not_applicable_to_critic

uncertainty_area: not_applicable_to_critic

model_capability_match: predicted_high for MonST3R on this regime (its native specialty per its paper); predicted_low for MASt3R / Fast3R / Spann3R on dynamic scenes. Result: A5 reroute_model has near-zero upside in this case; the policy correctly stays with MonST3R and acts via rerun_local_region or open_anchor_budget.

## Comparator Policies

| Policy | Action chosen | Predicted proxy P1 detection_rate | Notes |
| --- | --- | --- | --- |
| comparator_1 single-pass MonST3R (no critic) | emit per-frame outputs verbatim | 0 detection rate | baseline; no detection mechanism |
| comparator_2 Test3R-style verify-then-report | A4 only on each frame; report conflict score; no A5 | paper-proven detection rate in [0.5, 0.8] inferred for this regime (in-family critic at native strength) | the strongest comparator for this card; this is exactly the regime Test3R was designed for |
| comparator_3 TTT3R-style trigger | A4 + retrigger MonST3R weights via test-time training | paper-proven detection at TTT3R level + on-the-fly correction; out of scope per cycle 009 D2' (no training) | cited for completeness; not a viable cycle 009 comparator |
| comparator_4 dynamic-mask self-vote | use MonST3R's own dynamic mask agreement as proxy critic; emit conflict where mask flips | paper-derived in-family heuristic; detection rate inferred [0.4, 0.7] | the cheapest in-family critic; weak on cross-window drift |
| dream_policy A4 -> A5 closed loop | A4 on cross-model evidence vector (pointmap_conflict + confidence_drop + reprojection_residual; reads MonST3R dynamic mask as one signal among several); A5 sub-action selection per window: rerun_local_region if conflict is local + no drift signal; open_anchor_budget if Memory's latent_drift_proxy flags drift; A5 reroute_model NOT chosen due to low capability_match for alternatives | predicted detection rate [0.6, 0.9] inferred (paper-derived bound, not measured); each detection carries an attached A5 sub-action label | the differentiator vs comparator_2 is sub-action attachment, not raw detection rate; vs comparator_4 is multi-signal robustness; vs comparator_3 is "no training" boundary |

## Predicted Proxy Outcome

primary_metric: P1 conflict detection (per `specs/SPEC-20260503-001-geometry-critic.md` line 201)

unit_of_measurement: per-frame detection_rate over labeled conflict frames in [0, 1]; per-frame false_alarm_rate over labeled non-conflict frames in [0, 1]

threshold_for_useful_signal: detection_rate >= 0.6 AND false_alarm_rate <= 0.5 (per spec acceptance_threshold lines 217-225); this card is the primary load-bearer for that threshold

predicted_dream_value: detection_rate inferred [0.6, 0.9] (paper-derived bound from MonST3R + Test3R combined coverage on dynamic-scene regime); false_alarm_rate theta_conflict-dependent and inferred <= 0.4 with adequate tuning

predicted_best_comparator_value: comparator_2 Test3R-style detection_rate inferred [0.5, 0.8] on this native regime; comparator_4 dynamic-mask self-vote detection_rate inferred [0.4, 0.7]

expected_gap_direction: dream_better against comparator_1 / comparator_4 unconditionally; comparable or marginally dream_better against comparator_2 on detection rate; dream_better against comparator_2 on action attachment (comparator_2 emits orphan reports; dream emits report + action)

decision_signal_meaning:

```text
The detection-rate gap between dream_policy and comparator_2 is predicted
narrow on this regime. If the gap is zero or negative on detection rate
alone, the spec does NOT immediately fail - the spec's primary differentiator
is "verify-then-act vs verify-then-report" (line 113-114), which is an
action-attachment claim, not a detection-rate claim. The fail condition for
this card is: every detected conflict in dream_policy carries an attached
A5 sub-action label (rerun_local_region or open_anchor_budget); zero orphan
reports across the 48 frames. If even one labeled conflict comes with an
empty A5 slot, the spec's weakest_pressure_point fires for that frame and
the pattern across frames must be examined.

A secondary signal: the per-frame split between "rerun_local_region" and
"open_anchor_budget" should be non-degenerate (both sub-actions fire on
some subset of frames). If either sub-action never fires, the policy is
effectively single-action and the action-attachment differentiator weakens
to "always rerun" or "always open budget".
```

## Cross-Spec Contract Usage (CR-6)

Recorded per Cross-Spec Signal Contract rule CR-6 (`paradigm/CROSS_SPEC_SIGNAL_CONTRACT.md` line 149).

- CR-1 (Critic A5 reroute_model requires Composer agreement on capability_match spread): not exercised in this card. capability_match for cross-3R-model alternatives is predicted_low (MonST3R is the native dynamic-scene specialist), so A5 reroute_model is not the chosen sub-action and CR-1 does not fire. Cross-card load distribution: CR-1 was exercised in CASE-20260504-CRITIC-02.
- CR-3 (Memory drift signal does not gate Critic verification): **exercised in this card**. Critic A5 reads Memory's latent_drift_proxy as a cross-spec read to decide between rerun_local_region and open_anchor_budget. Per CR-3, this read does NOT gate A4 verification - A4 still fires from in-scope signals. The CR-3 honoring is visible in the Evidence Signals section: latent_drift_proxy is marked read_only_for_critic, not a gating input. If Memory had not drafted CASE-20260504-MEMORY-* yet (cycle 009 default schedule has Memory cards in cycle 010 per cycle 008 D1), the latent_drift_proxy read returns a forward-reference null and the policy falls back to rerun_local_region.
- CR-4 (Composer route_recommendation does not bind Critic on capability_match ties): trivially honored. No tie exists; alternatives are predicted_low.
- CR-5 (All cross-spec signals carry producer evidence label): honored. latent_drift_proxy carries Memory's evidence_label (forward-reference until Memory cards land); Critic's conflict_score carries Critic's own evidence_label.
- CR-6 (cycle 009 case cards record contract usage): satisfied by this section.

## Writing Value

related_work_section: critic

figure_or_taxonomy: extends the two-panel timeline of `specs/SPEC-20260503-001-geometry-critic.md` lines 252-257 - Panel A original MonST3R per-frame outputs across 48 frames (sparse strip); Panel B critic overlay with red zones on detected conflict frames + A5 sub-action label per frame (rerun_local_region vs open_anchor_budget) + per-frame route_regret bar near zero (no model switch). The non-degenerate split between rerun_local_region and open_anchor_budget across the 48 frames is the most teacher-visible differentiator from a Test3R-only timeline.

novelty_claim_supported:

```text
"Verify-then-act, not verify-then-report" (one_line_thesis line 114). On
this regime in-family Test3R-style critics already verify well; Dream's
contribution is action attachment per detected conflict. The card succeeds
if every detected conflict carries a concrete A5 sub-action and the split
between rerun_local_region and open_anchor_budget is non-degenerate. The
card fails the differentiator test if Dream's labels are isomorphic to
comparator_2's labels - same detections, same orphan reports.
```

## Risk And Boundaries

fail_fast_condition:

```text
If across the 48 frames either (a) detection_rate < 0.6, or (b)
false_alarm_rate > 0.5, or (c) any detected conflict has no attached A5
sub-action, or (d) only one A5 sub-action fires across all detected
conflicts (single-action policy), the spec's fail_fast_threshold lines
229-236 fires for this card. Among these, (c) is the most informative -
it directly contradicts the verify-then-act claim.
```

reproduction_required: no

training_required: no

frontend_change_required: no

approval_required: no

caveats:

```text
- Every predicted value above is paper-derived or inferred. No KYKT job
  artifact has been opened; no MonST3R output has been read; no manual
  per-frame labels exist yet for this case. Per D2' (paper-derived) this
  is the correct boundary for cycle 009.
- The detection-rate range [0.6, 0.9] is paper-derived from MonST3R's
  own confidence reporting + Test3R's detection claims; it is not a
  measurement on this specific 48-frame job. Manual labeling in a
  follow-up cycle (which is the natural upgrade path: paper-derived ->
  manual labels -> code-observed) will resolve the range one frame at
  a time.
- The cross-spec read of Memory's latent_drift_proxy is a forward
  reference until Memory case cards land. The card is written so that
  the absence of Memory cards collapses the policy to a single sub-action
  (rerun_local_region) without breaking the spec, but with a noticeably
  weakened differentiator. This is the explicit cost of the cycle 009 D1
  ordering (Critic before Memory) and is recorded here so the cost is
  visible.
- Card 03's role in the three-card portfolio: card 01 stress-tests the
  no-reroute regime (static pair); card 02 stress-tests the reroute
  regime (Fast3R vs Spann3R) and exercises CR-1; card 03 stress-tests
  the dynamic-scene native-comparator regime and exercises CR-3.
  Together the three cards exercise CR-1 + CR-3 + CR-4 + CR-5 + CR-6;
  CR-2 (Permanence -> Memory) is not Critic-owned and is not exercised
  by these three cards.
```

## Next Action

next_action: escalate_to_spec (the three Critic L2 cards now constitute the L2 evidence packet for SPEC-20260503-001; next move per spec line 448 is `fill_case_cards` -> done; subsequent move is cycle 009 S5 Composer cards plus S6 contract usage audit)

linked_next_artifact: `cases/CASE-20260505-COMPOSER-01.md` (first Composer capability card per `specs/SPEC-20260504-001-3r-composer.md` line 272 reservation; supplies the Composer side of CR-1 referenced from CASE-20260504-CRITIC-02; scheduled in cycle 009 S5)
