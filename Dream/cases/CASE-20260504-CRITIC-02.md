# CASE-20260504-CRITIC-02

Second Critic L2 case card per `specs/SPEC-20260503-001-geometry-critic.md` line 209 reservation. Paper-derived per cycle 009 D2' (`decisions/DEC-20260504-003-cycle-009-launch.md`). No measurement claimed; no model invocation; no KYKT job artifact opened. All numeric ranges below are paper-derived or inferred and labeled accordingly per Cross-Spec Signal Contract rule CR-5.

This card is the explicit route_regret spread carrier for the Critic spec acceptance_threshold (`specs/SPEC-20260503-001-geometry-critic.md` line 224 "route_regret on Fast3R vs Spann3R route choice has nonzero spread"), and the explicit exercise of Cross-Spec Signal Contract rule CR-1 (Critic A5 reroute_model requires Composer agreement on capability_match spread).

## Identity

case_id: CASE-20260504-CRITIC-02

proxy_id: P5 (primary for this card; secondary for the spec)

scenario_name: Fast3R vs Spann3R route comparison on KYKT jobs 20260425-113002 vs 20260425-113227

date: 2026-05-04

linked_failure_modes: F3 | F6

linked_actions: A4 | A5

linked_branches: Geometry_Critic

linked_research_units: RU-003 | RU-011 | RU-002 (support; A5 reroute_model leans on Composer capability cards per spec line 423)

linked_sources: Fast3R | Spann3R | Test3R | CTRL (see `registry/source_registry.md` for canonical IDs)

## Input Artifact

source_type: paper_example

artifact_pointer:

```text
- model_uploads/20260425-113002/  (Fast3R run)
- model_uploads/20260425-113227/  (Spann3R run)
The two jobs target the same input regime; the ~24 minute timestamp gap
suggests a deliberate side-by-side route comparison submitted by the user.
Per D2' the input contents are not opened for this card.
```

evidence_label: paper-proven (for Fast3R and Spann3R capability profiles as documented in their respective papers); inferred (for every Dream Critic A4 -> A5 prediction in this card, including the predicted route_regret spread sign and magnitude)

input_summary:

```text
Fast3R and Spann3R have paper-proven distinct capability sweet spots:
Fast3R targets large-batch single-forward-pass reconstruction across many
frames; Spann3R targets streaming / online reconstruction with explicit
spatial memory. For any given input regime, the paper-claimed capability
profiles produce a non-zero capability_match spread. This case is the
Critic spec's primary regime for the cross-model A5 reroute_model action,
since the static-pair regime in CASE-20260504-CRITIC-01 left A5 with no
viable reroute target. Without opening the artifact the actual regime
(streaming vs batch) is unknown to this card; the prediction below is
written conditionally on the regime, and the route_regret spread sign
flips with regime.
```

## Evidence Signals

Recorded values are predicted Critic readings under D2' paper-derived analysis. Numeric scales are illustrative.

pose_novelty: predicted_present (multi-frame sequence; both Fast3R and Spann3R operate on multi-frame inputs)

view_overlap: predicted_variable across the sequence (paper-derived: both papers report capability degradation as overlap drops, with different break points)

reprojection_residual: predicted_higher on the lower-capability-match model's output (paper-proven mechanism; the magnitude per regime is inferred)

pointmap_conflict: predicted_present on whichever model is in the wrong-regime. For streaming regime: pointmap_conflict predicted_higher on Fast3R (no temporal memory). For batch / disconnected-image regime: pointmap_conflict predicted_higher on Spann3R (memory expects continuity).

confidence_drop: predicted_asymmetric between the two models (paper-derived: each model's confidence map calibrates against its own regime)

latent_drift_proxy: not_applicable_to_critic (Memory spec scope per spec line 182)

dynamic_ratio: not_applicable_to_critic (Permanence spec scope)

optical_flow_conflict: not_applicable_to_critic (Permanence spec scope)

object_track_stability: not_applicable_to_critic

loop_candidate_score: not_applicable_to_critic

anchor_importance: not_applicable_to_critic

cache_pressure: not_applicable_to_critic

external_memory_overlap: not_applicable_to_critic (Spann3R DOES have spatial memory, but Critic does not own that signal; Memory spec ownership)

prior_rgb_conflict: not_applicable to this case (no Cross-Modal prior interaction)

blur_or_low_light_score: not_applicable_to_critic

uncertainty_area: not_applicable_to_critic

model_capability_match: predicted_spread between Fast3R and Spann3R for the same regime, in [0.2, 0.6] absolute spread on a 0-1 capability scale (paper-derived bounds; magnitude inferred). The spread sign is regime-dependent: streaming -> Spann3R higher; batch -> Fast3R higher.

## Comparator Policies

| Policy | Action chosen | Predicted proxy P5 (route_regret) value | Notes |
| --- | --- | --- | --- |
| comparator_1 always-Fast3R (no critic) | emit Fast3R output verbatim | route_regret = 0 if regime is batch, route_regret = capability_spread if regime is streaming | baseline; no detection |
| comparator_2 always-Spann3R (no critic) | emit Spann3R output verbatim | route_regret = 0 if regime is streaming, route_regret = capability_spread if regime is batch | symmetric baseline |
| comparator_3 Test3R-style verify-then-report | A4 only on whichever model ran; report conflict; no reroute | route_regret unchanged from input policy (no model switch) | in-family critic cannot reroute |
| comparator_4 capability-card lookup pre-route (no critic) | route by static input metadata heuristic | route_regret >= 0 (depends on heuristic match to actual regime) | the pre-routing baseline; cannot self-correct |
| dream_policy A4 -> A5 closed loop | A4 fires on the losing model's pointmap_conflict + confidence_drop; A5 chooses reroute_model after CR-1 check with Composer; CR-1 requires capability_match spread above threshold tau_spread | route_regret predicted near 0 (Critic picks the higher-capability_match model after CR-1 confirms spread); fail mode if Composer vetoes due to tied capability_match | this is the spec's main differentiator vs comparator_3 (in-family) and vs comparators 1/2/4 (no critic loop) |

## Predicted Proxy Outcome

primary_metric: P5 route_regret (per `specs/SPEC-20260503-001-geometry-critic.md` line 203 secondary_proxy; primary for this card)

unit_of_measurement: gap between chosen route capability_match and best-known route capability_match for this regime (`specs/SPEC-20260503-001-geometry-critic.md` line 187), in [0, 1]

threshold_for_useful_signal:

```text
spec acceptance_threshold line 224 requires "route_regret has nonzero
spread across the three case cards". This card carries the load on
nonzero spread; if route_regret here is 0 in all comparator rows, the
spec fails its acceptance gate.
```

predicted_dream_value: predicted near 0 (Critic picks the higher-capability_match model after CR-1 confirms spread is above tau_spread); inferred from paper capability profiles, not measured

predicted_best_comparator_value: predicted equal to capability_match spread for the wrong-regime fixed baseline (comparator_1 or comparator_2, whichever ran the wrong model); predicted between 0 and capability_match spread for comparator_4 depending on heuristic accuracy

expected_gap_direction: dream_better against comparator_1 / comparator_2 in their wrong-regime case; tie or dream_better against comparator_3 (which cannot reroute at all so route_regret stays at input value); ambiguous against comparator_4 absent regime label accuracy

decision_signal_meaning:

```text
This is the highest-leverage card for the Critic spec. If route_regret
collapses to 0 across all comparators (i.e. capability_match spread is
inferred near 0), the spec's "cross-model A5 reroute differentiator"
disappears and the spec retires per fail_fast_threshold. If Composer
vetoes the reroute via CR-1 (capability_match tied within tau_spread),
the dream_policy correctly falls back to comparator_3 behavior on this
window and that fallback is itself a positive signal (CR-1 prevented
spurious reroutes). The card's value is the spread sign + magnitude,
not the absolute detection rate (which is the job of card 01 and 03).
```

## Cross-Spec Contract Usage (CR-6)

Recorded per Cross-Spec Signal Contract rule CR-6 (`paradigm/CROSS_SPEC_SIGNAL_CONTRACT.md` line 149).

- CR-1 (Critic A5 reroute_model requires Composer agreement on capability_match spread): **exercised in this card**. Predicted interaction:
  1. Critic publishes proposed_reroute = {from: losing_model, to: winning_model, spread_estimate: paper_derived_value, evidence_label: inferred}.
  2. Composer reads its capability cards, returns agree | veto with spread_above_tau_spread boolean.
  3. If agree: Critic decrements repair_budget, emits A5 reroute_model action, logs route_regret_estimate.
  4. If veto: Critic falls back to emit conflict_unresolved + surfaces to Advisor (no model switch).
  5. The Composer capability cards themselves are paper-derived per cycle 009 D2'; CASE-20260505-COMPOSER-01..03 are the canonical capability-card sources (per `specs/SPEC-20260504-001-3r-composer.md` line 272 reservation).
- CR-3 (Memory drift signal does not gate Critic verification): trivially honored. No latent_drift_proxy input is read by Critic A4.
- CR-4 (Composer route_recommendation does not bind Critic on capability_match ties): explicitly relevant here. If CR-1 returns Composer veto, the dream_policy does NOT then invoke a Composer route_recommendation override; CR-4 prevents that loophole. The fallback is conflict_unresolved, not "Composer's preferred route".
- CR-5 (All cross-spec signals carry producer evidence label): honored. capability_match spread carries Composer's evidence_label (paper-derived per D2'); Critic's conflict_score carries Critic's own evidence_label.
- CR-6 (cycle 009 case cards record contract usage): satisfied by this section.

## Writing Value

related_work_section: critic

figure_or_taxonomy: extends the two-panel timeline of `specs/SPEC-20260503-001-geometry-critic.md` lines 252-257 — Panel A side-by-side Fast3R + Spann3R outputs on the same regime; Panel B critic overlay highlighting the predicted reroute_model action with route_regret bar showing capability_match spread that justified the switch. CR-1 hand-off arrow from Critic to Composer makes the contract visible.

novelty_claim_supported:

```text
"Cross-model A4 -> A5 closed loop, with explicit cross-spec contract
arbitration" - this is the card that defends the spec against the read
"Dream Critic = Test3R + always-reroute heuristic". The CR-1 check is
what distinguishes the policy from a free-running router: Composer can
veto, and the policy respects the veto rather than overriding via CR-4
loophole. Card 01 (static pair) showed reroute is not always available;
this card shows reroute is gated by an explicit cross-spec contract,
not by a unilateral Critic decision.
```

## Risk And Boundaries

fail_fast_condition:

```text
If predicted route_regret is 0 across all comparator rows (i.e.
capability_match spread is inferred near 0 for this regime), the spec's
acceptance_threshold line 224 fails on this card and the spec retires.
```

reproduction_required: no

training_required: no

frontend_change_required: no

approval_required: no

caveats:

```text
- Every predicted value above is paper-derived or inferred. No KYKT job
  artifact has been opened; per D2' the input regime is not labeled in
  this card. The card is written conditionally on the regime so that
  later artifact opening + manual labeling can resolve the conditional
  one signal at a time.
- The predicted capability_match spread magnitude [0.2, 0.6] is paper-
  derived from the Fast3R and Spann3R papers' own capability claims; it
  is not a measurement on these specific KYKT jobs. When card 02 is
  later upgraded with manual labels, the spread becomes code-observed
  on this regime specifically.
- This card depends on the existence of paper-derived Composer capability
  cards (CASE-20260505-COMPOSER-01..03) for CR-1 to actually be checkable.
  The Composer cards are scheduled in cycle 009 S5; until they land, the
  CR-1 check in this card is a forward reference, labeled inferred.
- CR-4 is named explicitly in this card precisely because it is the
  loophole that would weaken CR-1; treating CR-4 as a passive default
  would let "Composer veto -> Composer route_recommendation -> route
  switch anyway" sneak in. Recording CR-4 here closes that hole.
```

## Next Action

next_action: add_comparator

linked_next_artifact: `cases/CASE-20260504-CRITIC-03.md` (MonST3R 48-frame per-frame conflict labels; the per-window detection-rate carrier, complementary to this card's spread-carrier role and to card 01's static-pair adversarial role)
