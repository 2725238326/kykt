# CASE-20260504-MEMORY-03

Last updated: 2026-05-04 (cycle 010 S4 second half; secondary Memory L2 case card; MASt3R small-N baseline; bounds Memory behavior on a regime where memory is not the headline value-add)

## Identity

case_id: CASE-20260504-MEMORY-03

spec_id: SPEC-20260503-002 (Executive Memory)

cycle: 010

evidence_label: paper-proven (for the MASt3R upstream artifact: matches.png + pointcloud.ply on a static pair as documented in the MASt3R paper); inferred (for the predicted Dream Memory behavior on a small-N static-pair regime; the central prediction is that Dream Memory's A1/A2/A3 actions reduce to a small near-trivial set on this regime, which is NOT a failure but a correct boundary-of-applicability result).

paper_only: yes (cycle 010 D2' default).

contract_version: **v2** (Memory does not consume cost-axis signals; CR-4 trivially honored).

## Input Artifact

job_id: 20260420-222729

upstream_model: MASt3R

upstream_outputs available:

- matches.png (per-pair feature matches visualization)
- pointcloud.ply (consolidated points from the static pair)

window structure used by Memory:

```text
A static pair = 2 frames; one window. There is no streaming timeline
across which Memory can exercise A1/A2/A3 in a non-trivial way. This
card explicitly tests Memory on the regime where it is NOT value-adding,
to bound the policy's behavior cleanly: Dream Memory should NOT
hallucinate work on inputs where there is nothing to govern.
```

input regime classification:

```text
regime_card(input) = {
  static_pair       : 0.95 (clearly the headline regime)
  many_view         : 0.05
  streaming         : 0.00
  dynamic_video     : 0.00
  static_collection : 0.00
}
all probabilities inferred; sum = 1.00
```

## Why This Card Exists (Compared To MEMORY-01 + MEMORY-02)

MEMORY-01 carries the headline A1/A2/A3 differentiation argument on a 48-frame dynamic regime. MEMORY-02 carries the externalization-of-governance argument on a streaming-with-memory regime. This card tests the **boundary-of-applicability**: on a static-pair regime where there is no temporal context to govern, does Dream Memory's policy correctly reduce to trivial?

The answer this card argues for (paper-derived, inferred):

```text
Dream Memory on a static-pair regime should:
  - A1: skip_update most likely (no pose_novelty across a pair; no
    confidence_drop across windows that don't exist; update_pressure
    is trivially zero)
  - A2: write at most one entry (a single static-pair pointcloud); no
    merge / defer / ignore (no candidates beyond the one)
  - A3: protect anchor on the single pointcloud entry; cache_pressure
    trivially low; no eviction; no global context request
  - latent_drift_proxy: trivially flat / undefined across one window
  - policy_log: 1-3 entries total, not the dozens of entries MEMORY-01
    or MEMORY-02 produce
This is the correct behavior. It is NOT a failure. The card succeeds
if and only if Dream Memory does not hallucinate work that the input
does not warrant.
```

## Evidence Signals

active_signals consumed (subset of MEMORY-01's vector; most evaluate trivially):

```text
pose_novelty                     trivially zero / undefined across one pair
view_overlap                     well-defined (it's the headline MASt3R signal)
confidence_drop                  trivially undefined across windows that
                                 don't exist
latent_drift_proxy               trivially undefined; flat
loop_candidate_score             trivially zero (no recurrence in 2 frames)
anchor_importance                well-defined for the single static-pair
                                 pointcloud entry
cache_pressure                   trivially low
external_memory_overlap          trivially zero
```

cross-spec read signals: all trivially honored or null.

published outward:

```text
latent_drift_proxy: trivially flat
anchor_set: {single_pointcloud_anchor} (size 1)
policy_log: 1-3 entries (A1 skip_update; A2 write; A3 protect)
```

## Comparator Policies

| Policy | Behavior on static-pair | Predicted P2 / P3 vs GEM-3R |
| --- | --- | --- |
| comparator_1 confidence_only | retain by confidence; on a single pair, retains the single output | P2 trivially equal to GEM-3R |
| comparator_2 fixed_sliding_window | window of 8 but only 2 frames; degenerates to "retain both" | P2 trivially equal to GEM-3R |
| comparator_3 write_everything | writes both frames + the pointcloud | P2 trivially equal; P3 duplicate higher (writes redundant entries) |
| dream_policy GEM-3R | 1-3 policy_log entries; A1 skip_update; A2 write once; A3 protect once | P2 trivially equal to comparators 1 + 2; P3 duplicate <= comparator_3 |

On a static-pair regime, all reasonable policies converge. GEM-3R's success criterion here is **non-degeneration**: it does not invent work that does not exist.

## Predicted Proxy Outcome

primary_metric: P2 anchor_retention (degenerate on this regime; all policies retain the single anchor)

secondary_metric: policy_log non-hallucination (Dream-specific; not in spec's P2/P3 list but the relevant axis on this regime)

predicted_dream_value:

- P2 anchor_retention: 1.0 (single anchor; trivially retained)
- P3 reuse_rate: 0.0 / undefined (no recurrence to reuse)
- P3 duplicate_rate: 0.0 (no duplicate writes attempted)
- policy_log size: 1-3 entries (predicted; non-hallucination axis)

predicted_best_comparator_value: comparators 1 + 2 + 3 all converge on P2 = 1.0; comparator_3 fails on the policy_log non-hallucination axis if it writes redundant entries.

expected_gap_direction: dream_tied on P2 with comparators 1 + 2; dream_better on the non-hallucination axis vs comparator_3.

decision_signal_meaning:

```text
The decisive signal on this card is that Dream Memory's policy_log is
short (1-3 entries) on a static-pair regime. If GEM-3R hallucinates
work (e.g. A2 fires multiple times; A3 evicts something; latent_drift_
proxy spikes), the policy is over-engineered for static cases and that
is a Discipline rule 2 (Minimum Viable Mechanism) violation worth
recording.
```

## Cross-Spec Contract Usage (CR-6)

Recorded per Cross-Spec Signal Contract rule CR-6 v2.

- CR-1: trivial.
- CR-2: not exercised (no Permanence card on this static-pair job in cycle 010 — `CASE-20260504-PERMANENCE-02` is on the MASt3R static pair too, but it is a static-control card and emits zero suppress_static_write signals; CR-2 is trivially honored from both sides on this regime).
- CR-3: this card publishes a flat / undefined latent_drift_proxy. No Critic card consumes it on this job.
- CR-4: trivial under v2.
- CR-5: honored. The published flat trace carries `paper-proven` evidence on the qualitative shape (no drift to publish on a single pair) and `inferred` on the absolute null value treatment.
- CR-6: satisfied.

## Writing Value

related_work_section: memory (boundary-of-applicability appendix)

figure_or_taxonomy: small comparison panel showing GEM-3R policy_log size across the three Memory cards (MEMORY-01 dozens of entries on dynamic-video; MEMORY-02 multiple entries on streaming; MEMORY-03 1-3 entries on static-pair). The trend visualizes the controller's regime-adaptivity.

novelty_claim_supported:

```text
"Dream Memory's policy_log entropy correctly tracks the regime's
governance demand" (a corollary of the boundedness rule in spec line 109).
The three Memory cards together support the claim: dozens of entries on
dynamic-video, multiple on streaming, 1-3 on static-pair. The card fails
the corollary only if Dream Memory hallucinates work on the static-pair
input.
```

## Risk And Boundaries

fail_fast_condition:

```text
This card fails if:
  (a) Predicted Dream Memory hallucinates work on the static pair (more
      than ~3 policy_log entries; A2 fires more than once; A3 evicts
      something that is not there to evict). This is the spec
      Discipline rule 2 violation flag.
  (b) Any predicted value reads as paper-proven where it should be
      inferred.
```

scope_boundaries:

```text
This card does NOT:
  - establish any positive A1/A2/A3 differentiation argument; that lives
    in MEMORY-01
  - extend MEMORY-02's externalization-of-governance argument
  - close any cycle 009 contract gap (G1/G2/G3); those close in
    PERMANENCE-01 + cycle 010 v2 adoption
  - drive any spec retirement decision on its own; the static-pair
    regime is acknowledged as out-of-scope for the Memory headline
    argument
```

## Next Action

immediate next case card: `cases/CASE-20260504-PERMANENCE-02.md` (cycle 010 S5; MASt3R static-pair static control; closes PERMANENCE-01's fail_fast condition (c) "no hallucinated tracks on static input").

deferred to cycle 010 closeout S6: this card's contract audit row is single-line (all CR rules trivial / honored).
