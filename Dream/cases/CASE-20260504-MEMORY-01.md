# CASE-20260504-MEMORY-01

Last updated: 2026-05-04 (cycle 010 S2; first Memory L2 case card; cross-pair partner CASE-20260504-PERMANENCE-01 drafted in same cycle under v2 contract; publishes latent_drift_proxy that closes cycle-009 CRITIC-03 forward-reference null)

## Identity

case_id: CASE-20260504-MEMORY-01

spec_id: SPEC-20260503-002 (Executive Memory)

cycle: 010

evidence_label: paper-proven (for MonST3R upstream artifact: 48 frames + 96 dynamic masks + 96 per-frame confidence + scene.glb + pred_traj.txt as documented in the MonST3R paper); inferred (for every Dream Memory A1 / A2 / A3 sub-action attachment per window, every threshold value, and every magnitude prediction in this card).

evidence_label_propagation: per cross-spec contract CR-5 (`paradigm/CROSS_SPEC_SIGNAL_CONTRACT.md` v2), each cross-spec read carries the producer's evidence_label. This card consumes `suppress_static_write(r)` and `dynamic_ratio(r,t)` from Permanence; both arrive carrying Permanence's evidence_label (paper-proven on MonST3R-derived dynamic-region magnitudes per CASE-20260504-PERMANENCE-01; inferred on the per-window suppression decision boundaries).

paper_only: yes (cycle 010 D2' default carried forward from cycle 009; no KYKT-job-derived per-window measurement is performed).

contract_version: **v2** (cycle 010 case cards are v2-default per `decisions/DEC-20260504-005-cycle-010-launch.md`; MEMORY-01 does not consume Composer's `cost_adjusted_match` in v2 because Memory does not own A5; CR-4 is non-Memory-owned and trivially honored here).

## Input Artifact

job_id: 20260420-222928

upstream_model: MonST3R

upstream_outputs available (per cycle 009 Resource Inventory):

- 48 frames (input video sequence; dynamic content per MonST3R paper)
- 96 dynamic masks (per-frame; produced by MonST3R)
- 96 per-frame confidence arrays (per-frame; produced by MonST3R)
- scene.glb (consolidated 4D-aware scene asset)
- pred_traj.txt (predicted camera trajectory across the 48 frames)

window structure used by Memory:

```text
Memory operates on overlapping windows over the 48-frame stream. Default
window = 8 frames with stride 4, giving ~10 windows. This card uses
window_index t in {1, 2, ..., 10} for prediction granularity. Each
window produces one A1 decision, up to one A2 decision per candidate
write proposed in that window, and an A3 anchor_set / cache_window /
context_request update.
```

input regime classification (for cross-spec consumption by Composer):

```text
regime_card(input) = {
  static_pair        : 0.00 (48 frames, not a pair)
  many_view          : 0.10 (48 frames is borderline; MonST3R chose dynamic_video framing)
  streaming          : 0.25 (sequential timeline; some streaming traits)
  dynamic_video      : 0.65 (dominant; 96 dynamic masks indicate per-frame motion)
  static_collection  : 0.00
}
all probabilities inferred; sum = 1.00
```

## Evidence Signals

active_signals consumed by Memory in this card (per `specs/SPEC-20260503-002-executive-memory.md` lines 185-194):

```text
pose_novelty(t)              from pred_traj.txt + window-local SE(3) delta
view_overlap(t)              from scene.glb projection across consecutive windows
confidence_drop(t)           from per-frame confidence arrays (MonST3R-published)
latent_drift_proxy(t)        Memory-OWNED publication (this card emits it; consumed
                             by Critic CR-3 forward-reference from CASE-20260504-
                             CRITIC-03)
loop_candidate_score(t)      from view_overlap recurrence across windows
anchor_importance(t)         derived from loop_candidate_score + confidence
cache_pressure(t)            running cache_window size vs cache_budget
external_memory_overlap(t)   trivially zero in cycle 010 (no external memory store)
```

cross-spec read signals (read-only by Memory; producer evidence_label propagates per CR-5):

```text
dynamic_ratio(r, t)          from CASE-20260504-PERMANENCE-01 (cross-pair partner;
                             paper-proven on MonST3R-derived per-frame dynamic
                             coverage; inferred on per-region partition)
suppress_static_write(r)     from CASE-20260504-PERMANENCE-01 (CR-2 binding;
                             paper-proven trigger condition; inferred on per-region
                             suppression decision)
conflict_score(t)            forward-reference null this card; cycle 009 CRITIC-03
                             would publish per-window conflict on this same job,
                             but CRITIC-03 referenced future Memory cards (this
                             card!), not the other way around. Memory's A1
                             skip_update fallback fires from conflict_score read
                             only when CR-3 path is live; cycle 010 S6 audit
                             closes this loop.
```

derived_signals computed by Memory:

```text
update_pressure(t)     = pose_novelty + confidence_drop + latent_drift_proxy(t-1)
write_value_estimate   = loop_candidate_score + anchor_importance + external_memory_overlap - duplicate_rate
context_demand         = cache_pressure + conflict_score (read; null fallback) + drift
```

published outward (consumed by other specs):

```text
latent_drift_proxy(t)        Memory's headline cross-spec publication. This card
                             projects a non-monotonic trace across the 48-frame
                             timeline: low at frames 1-8 (anchor establishment),
                             rising at frames 16-24 (dynamic activity per MonST3R
                             dynamic masks), partial recovery at frames 32-40
                             (loop candidates re-anchor), final rise at frames
                             40-48 (drift dominates). Specific values are
                             inferred; the qualitative non-monotonic shape is the
                             prediction.
anchor_set(t)                set of protected anchor indices; A3 maintains this
cache_window(t)              bounded sliding cache contents; informational
policy_log(t)                append-only trace of A1/A2/A3 sub-action picks
```

## Comparator Policies

| Policy | A1 behavior | A2 behavior | A3 behavior | Predicted P2 anchor_retention / P3 reuse_rate / P3 duplicate_rate vs GEM-3R |
| --- | --- | --- | --- | --- |
| comparator_1 confidence_only | always full_update if confidence above threshold; skip_update otherwise | write iff confidence above threshold; no merge; no defer | retain by confidence rank only; no loop-candidate protection; no cache evict policy | P2 lower (loses loop closures Memory protects); P3 reuse lower (no anchor protection means recurring views overwrite); P3 duplicate similar (no merge but also no spurious writes) |
| comparator_2 fixed_sliding_window (window=8) | always full_update | always write within window | retain last 8 frames; evict by recency | P2 much lower (cannot retain across 48 frames; loses any loop > 8 frames apart); P3 reuse near-zero (eviction precedes reuse); P3 duplicate higher (no merge, repeated motion regions written each window) |
| comparator_3 write_everything | always full_update | always write; never merge; never defer | no eviction; cache grows unboundedly | P2 trivially highest (everything retained); P3 reuse trivially highest; P3 duplicate dramatically higher than GEM-3R; cache_pressure unbounded so the policy fails fail_fast on engineering grounds rather than P2/P3 grounds |
| comparator_4 uniform_keyframe (every K=4 frames) | full_update at keyframes; skip_update otherwise | write at keyframes; no merge; no defer | retain keyframes; no loop protection | P2 medium (keyframes catch some loops by luck); P3 reuse medium; P3 duplicate moderate (keyframe spacing reduces but does not dedupe) |
| dream_policy GEM-3R (this card's prediction) | per-window mix: full_update on low-dynamic + high-update_pressure windows; pose_adaptive_update on medium update_pressure; kalman_update on partial-conflict windows; skip_update on high-dynamic windows; reset_state predicted not to fire on this 48-frame run (drift not persistent over enough windows) | per-window mix: write on high write_value_estimate + low duplicate_rate; merge on duplicate_rate spikes around frame 24-32 (loop region); ignore when CR-2 suppress_static_write fires from PERMANENCE-01 cross-pair on dynamic regions; defer at borderline windows 8-12 and 36-40 | protect anchor on loop candidates around frames 8-12 and 32-40; evict cache when cache_pressure exceeds budget at frames ~28; request global context predicted not to fire (no external store in cycle 010) | P2 GEM-3R >= comparator_1 AND >= comparator_2 (acceptance threshold satisfied directionally; magnitude inferred); P3 reuse > uniform_keyframe (acceptance directionally satisfied); P3 duplicate <= write_everything (trivially; merge fires) |

## Predicted Proxy Outcome

primary_metric: P2 anchor_retention

unit_of_measurement: fraction of anchors retained across the 48-frame run that correspond to true loop candidates per pred_traj.txt-derived ground-truth-loops; in [0, 1]

threshold_for_useful_signal: per spec acceptance_threshold (lines 247-256), GEM-3R must satisfy:

```text
P2 anchor_retention(GEM-3R) >= anchor_retention(confidence_only)     [holds in this card by direction]
                            AND >= anchor_retention(fixed_sliding_window)  [holds in this card by direction; magnitude inferred]
P3 reuse_rate(GEM-3R) > reuse_rate(uniform_keyframe baseline)        [holds in this card by direction]
P3 duplicate_rate(GEM-3R) <= duplicate_rate(write_everything)        [holds trivially: merge fires; write_everything has no merge]
policy_log shows A1 / A2 / A3 each fire at least once AND not always with the same sub-action
```

This card's prediction satisfies all five acceptance criteria directionally (under inferred magnitudes); cycle 010 S6 audit revisits the directional claims under the v2 contract for any Composer-side cost-axis spillover (none expected: Memory does not consume cost_adjusted_match).

predicted_dream_value:

- P2 anchor_retention: ~0.75 (inferred; loop closures around frames 8-12 and 32-40 retained; some borderline anchors at frames 16-24 lost to dynamic suppression)
- P3 reuse_rate: medium-positive (~0.40 inferred; loop region 32-40 reuses anchors established at 8-12)
- P3 duplicate_rate: low (~0.10 inferred; merge fires at frames 24-32 to deduplicate dynamic-region candidate writes)

predicted_best_comparator_value:

- comparator_3 write_everything trivially wins on P2 (~1.0) but fails engineering fail_fast (cache unbounded; not a viable comparator on the operational axis); P2 win is a degenerate baseline, not a competitive one.
- comparator_1 confidence_only: P2 ~0.55 inferred; P3 reuse ~0.20 inferred; P3 duplicate ~0.10 inferred.
- comparator_2 fixed_sliding_window: P2 ~0.30 inferred; P3 reuse ~0.05; P3 duplicate ~0.20.
- comparator_4 uniform_keyframe: P2 ~0.50 inferred; P3 reuse ~0.30; P3 duplicate ~0.18.

expected_gap_direction: dream_better on P2 vs all non-degenerate comparators (1, 2, 4); dream_better on P3 reuse vs all comparators; dream_better on P3 duplicate vs comparator_3 (unbounded comparator); roughly tied on P3 duplicate vs comparator_1 (both have low duplicate but for different reasons — confidence_only filters writes; GEM-3R deduplicates).

decision_signal_meaning:

```text
The decisive signal is policy_log entropy: GEM-3R fires distinct A1 sub-
actions (full_update, pose_adaptive_update, kalman_update, skip_update)
across the 48-frame run, distinct A2 sub-actions (write, merge, ignore,
defer), and distinct A3 sub-actions (protect, evict, [request not fired]).
This is the "controller is not a renamed single policy" criterion from
spec line 254-255. If the prediction collapses to a single sub-action per
action category, Memory spec triggers fail_fast threshold (spec lines
260-267) and retires.
```

## Cross-Spec Contract Usage (CR-6)

Recorded per Cross-Spec Signal Contract rule CR-6 (`paradigm/CROSS_SPEC_SIGNAL_CONTRACT.md` v2 line 149).

- CR-1 (Critic A5 reroute_model requires Composer agreement on capability_match spread): not Memory-owned; trivially honored. Memory does not invoke A5 reroute.
- CR-2 (Permanence suppress_static_write is binding on Memory): **exercised in this card via in-cycle cross-pair**. PERMANENCE-01 (drafted in cycle 010 S3, same KYKT job) publishes `suppress_static_write(r)` for high-dynamic-ratio regions in frames 16-24. Memory's A2 reads this signal and applies it: A2 emits `ignore` on those candidate writes rather than admitting them. If Memory's own `write_value_estimate` would have admitted (high loop_candidate_score on a region overlapping the suppressed region), Memory still honors the suppression and logs `cross_spec_refusal{producer=Permanence, signal=suppress_static_write, reason=local_write_value_high}` to surface the conflict to Advisor. CR-2 closure: this card is the consumer side; PERMANENCE-01 is the producer side; both drafted in cycle 010 closes cycle 009 gap G1 (CR-2 zero coverage).
- CR-3 (Memory drift signal does not gate Critic verification): **producer-side closure of cycle 009 forward-reference null in this card**. Memory publishes `latent_drift_proxy(t)` over the 48 windows (qualitative non-monotonic trace described in Evidence Signals above). Cycle 009 CASE-20260504-CRITIC-03 forward-referenced this signal under a null-fallback protocol; cycle 010 S6 audit must verify that CRITIC-03's predicted A5 sub-action distribution remains consistent when Memory's actual published trace is read in place of null. Per CR-3, the value Memory publishes does NOT auto-trigger Critic A5; Critic still requires its own conflict_score path. CR-3 honored.
- CR-4 (Composer route_recommendation does not bind Critic on capability_match ties): not Memory-owned; trivially honored under v2 framing. Memory does not consume Composer's `cost_adjusted_match` axis.
- CR-5 (All cross-spec signals carry producer evidence label): honored. dynamic_ratio + suppress_static_write entries above carry Permanence's evidence_label inline. Memory's published latent_drift_proxy carries Memory's own evidence_label (inferred per-window magnitudes; paper-proven qualitative non-monotonic shape grounded in MonST3R confidence-trace behavior).
- CR-6 (cycle 010 case cards record contract usage under v2): satisfied by this section.

## Writing Value

related_work_section: memory

figure_or_taxonomy: extends the spec's Teacher Demo Form (lines 282-295) — the five-row MonST3R 48-frame timeline panel with A1 / A2 / A3 sub-action color coding per window. This card supplies the per-window predicted decisions populating Rows 3 / 4 / 5; the comparator-policy contrast table (lines 308) populates the side-by-side panels.

novelty_claim_supported:

```text
"Evidence-vector controller without learned policy can produce non-trivial
A1/A2/A3 differentiation on dynamic-regime data" (spec one_line_thesis
line 109: "memory is a budgeted controller, not a fixed cache"). This
card is the primary evidence carrier for that claim on the headline
MonST3R job. The card succeeds if the predicted policy_log entropy is
non-zero across A1, A2, and A3 (i.e. each action category fires more
than one sub-action). The card fails if the prediction collapses to
"GEM-3R is a renamed confidence_only policy" (which would trigger
spec fail_fast).
```

## Risk And Boundaries

fail_fast_condition:

```text
This card fails its differentiation test if any of:
  (a) Predicted A1 sub-action distribution collapses to a single sub-action
      across all 10 windows (e.g. always full_update; reduces to the
      always-update half of comparator_3).
  (b) Predicted A2 sub-action distribution collapses to a single sub-action
      (e.g. always write; reduces to comparator_3 entirely).
  (c) Predicted A3 sub-action distribution is empty (no anchor protected,
      no cache evicted; reduces to comparator_2 fixed-window without the
      window discipline).
  (d) The CR-2 cross-pair with CASE-20260504-PERMANENCE-01 fails to close
      in cycle 010 S6 audit (e.g. Permanence-01 declines to publish
      suppress_static_write on the dynamic frames 16-24, leaving
      cycle 009 gap G1 unclosed).
  (e) Any predicted value above (P2/P3 magnitudes) reads as paper-proven
      rather than inferred in a future review pass; the card violates
      Discipline rule 5 Honesty Override if so.
If any of (a)-(c) trigger from a future demo-observed re-derivation,
spec retires per spec lines 260-267. (a)-(c) are not triggered by THIS
card's prediction; they are the falsification axes the prediction
defends against.
```

scope_boundaries:

```text
This card predicts A1 / A2 / A3 sub-action attachment per window AND
predicts the qualitative shape of latent_drift_proxy(t) across the 48-
frame timeline. It does NOT:
  - run any inference (no MonST3R re-execution; no Memory module code;
    paper-derived only)
  - claim measured magnitudes for any P2 / P3 number (all magnitudes
    inferred; relative-ordering directionally inferred)
  - select a teacher demo target (D3 still deferred per
    decisions/DEC-20260504-002-no-all-in-on-single-finalist.md until
    cycle 010 closeout; this card contributes evidence to the eventual
    decision, does not force it)
  - bind Permanence's CR-2 producer-side decisions; PERMANENCE-01 is
    canonical for Permanence-side prediction
  - close Composer's CR-1 / CR-4 contract slots; those live in
    cycle 009 cards under v2-promoted COMPOSER-03
```

## Next Action

immediate next case card: `cases/CASE-20260504-PERMANENCE-01.md` (cycle 010 S3; same KYKT job 20260420-222928; CR-2 cross-pair partner; producer side of `suppress_static_write` consumed in this card).

deferred to cycle 010 closeout S6: contract usage audit verifies CR-2 closure between MEMORY-01 and PERMANENCE-01; CR-3 closure between MEMORY-01's published `latent_drift_proxy` and CRITIC-03's forward-reference fallback path; v2 spillover check (none expected; Memory does not consume cost-axis signals).

deferred to cycle 010 closeout S8: D3 first teacher demo target re-surfaced for user decision under full 4-finalist L2 coverage.
