__pycache__/
*.py[cod]
*.pyo

.idea/
.vscode/

.conda/
env/
venv/
.venv/

.DS_Store
Thumbs.db

# External upstream repositories and upload staging copies are kept locally only.
Coding/external_sources/

# Local model/weight staging. Large binary files must not be committed.
model_uploads/

# Temporary manual VS Code Server downloads should stay local only.
vscode-server-insiders-*.tar.gz

# Frontend build artifacts
node_modules/
dist/
*.tsbuildinfo
Coding/4.06/vision_ui/client/src-tauri/target/
Coding/4.06/vision_ui/client/vite.config.js

# Local release copies. Keep README files trackable, but never commit binaries.
release/**/*.exe
release/**/*.msi

# Experiment run outputs (logs, checkpoints, cloned repos). Keep local only.
Dream/experiments/runs/

# Downloaded wheel files. Never commit large binaries.
*.whl
Dream/planning/proposal_dream3r/awesome-ai-research-writing/
